# SearXNG 后端自动补全功能分析

## 概述

SearXNG 的自动补全功能从**三类独立数据源**获取建议，按照特定优先级规则进行合并，并在局部进行去重处理。整个流程的核心入口位于 [webapp.py#L811-L857](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py#L811-L857) 的 `autocompleter()` 路由函数。

---

## 一、多数据源获取机制

自动补全功能从以下三个独立数据源获取建议：

### 1. Answerers（内部即时回答系统）

**位置**：[searx/answerers/_core.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/answerers/_core.py)

**作用**：提供即时答案类的补全建议（如数学计算、统计数据、随机数等）。

**调用方式**：
```python
for obj in searx.answerers.STORAGE.ask(sug_prefix):
    if isinstance(obj, Answer):
        results.append(obj.answer)
```

**工作原理**：
- `AnswerStorage.ask()` 方法检查查询的第一个关键词是否匹配已注册 answerer 的关键词
- 如果匹配，则调用对应 answerer 的 `answer()` 方法获取结果
- 内置 answerer 包括：`random`（随机数生成）、`statistics`（统计信息）等
- 每次请求**都会**调用此数据源

### 2. 内部语法解析器自动补全

**位置**：[searx/query.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py)

**作用**：为 SearXNG 特有的查询语法提供自动补全。

**包含的解析器**：

| 解析器 | 触发前缀 | 补全内容 | 位置 |
|--------|----------|----------|------|
| `TimeoutParser` | `<` | 超时设置，如 `<3`（3秒）、`<850`（850毫秒） | [query.py#L43-L69](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L43-L69) |
| `LanguageParser` | `:` | 语言代码，如 `:en`、`:zh`、`:english` | [query.py#L72-L149](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L72-L149) |
| `ExternalBangParser` | `!!` | 外部 bang 命令，如 `!!g`、`!!ddg` | [query.py#L151-L175](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L151-L175) |
| `BangParser` | `!` | 引擎/分类名称，如 `!google`、`!images` | [query.py#L178-L237](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L178-L237) |

**工作原理**：
- `RawTextQuery` 初始化时自动调用各解析器
- 解析器识别查询中的特殊语法前缀（如 `!`, `:`, `<`, `!!`）
- 如果前缀匹配且启用了自动补全，则生成相关建议并存储在 `autocomplete_list` 中

### 3. 外部搜索后端

**位置**：[searx/autocomplete.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/autocomplete.py)

**作用**：从第三方搜索引擎获取搜索建议。

**可用后端**（共17个）：

| 后端名称 | 实现函数 | 数据源 |
|----------|----------|--------|
| `360search` | `qihu360search()` | 360搜索 |
| `baidu` | `baidu()` | 百度 |
| `bing` | `bing()` | 必应 |
| `brave` | `brave()` | Brave搜索 |
| `dbpedia` | `dbpedia()` | DBpedia |
| `duckduckgo` | `duckduckgo()` | DuckDuckGo |
| `google` | `google_complete()` | Google |
| `mwmbl` | `mwmbl()` | Mwmbl |
| `naver` | `naver()` | Naver |
| `quark` | `quark()` | 夸克 |
| `qwant` | `qwant()` | Qwant |
| `seznam` | `seznam()` | Seznam |
| `sogou` | `sogou()` | 搜狗 |
| `startpage` | `startpage()` | Startpage |
| `swisscows` | `swisscows()` | Swisscows |
| `wikipedia` | `wikipedia()` | 维基百科 |
| `yandex` | `yandex()` | Yandex |

**后端注册表**：
```python
backends: dict[str, t.Callable[[str, str], list[str]]] = {
    '360search': qihu360search,
    'baidu': baidu,
    'bing': bing,
    # ... 共17个后端
}
```

**调用方式**：
```python
def search_autocomplete(backend_name: str, query: str, sxng_locale: str) -> list[str]:
    backend = backends.get(backend_name)
    if backend is None:
        return []
    try:
        return backend(query, sxng_locale)
    except (HTTPError, SearxEngineResponseException):
        return []
```

**重要特点**：
- 用户在偏好设置中**只能选择一个**后端
- 支持多语言（部分后端如 `google_complete`、`duckduckgo`、`wikipedia` 会根据 `sxng_locale` 参数调整请求）
- 网络异常时静默失败，返回空列表

---

## 二、合并规则

### 核心逻辑

合并逻辑在 [webapp.py#L811-L857](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py#L811-L857) 中实现，遵循以下**优先级顺序**和**互斥规则**：

```
执行流程：
├─ 1. 收集 Answerers 结果（无条件执行）
├─ 2. 检查是否有内部语法补全结果 (autocomplete_list)
│   ├─ 有：添加内部语法补全结果（不调用外部后端）
│   └─ 无：调用外部搜索后端并添加结果
└─ 3. 返回最终结果
```

### 详细合并步骤

**步骤1：添加 Answerers 结果**（第825-827行）
```python
for obj in searx.answerers.STORAGE.ask(sug_prefix):
    if isinstance(obj, Answer):
        results.append(obj.answer)
```
- 每次请求都会执行
- 结果最先被添加到 `results` 列表

**步骤2：判断是否调用外部后端**（第831行）
```python
if len(raw_text_query.autocomplete_list) == 0 and len(sug_prefix) > 0:
```
- **关键互斥条件**：只有当 `autocomplete_list` 为空且查询非空时，才调用外部搜索后端
- 这意味着：如果用户输入了特殊语法前缀（如 `!`, `:`, `<`），则**不会**调用外部搜索后端

**步骤3：添加外部搜索后端结果**（第837-841行）
```python
for result in search_autocomplete(backend_name, sug_prefix, sxng_locale):
    if result != sug_prefix:
        results.append(raw_text_query.changeQuery(result).getFullQuery())
```

**步骤4：添加内部语法补全结果**（第843-845行）
```python
if len(raw_text_query.autocomplete_list) > 0:
    for autocomplete_text in raw_text_query.autocomplete_list:
        results.append(raw_text_query.get_autocomplete_full_query(autocomplete_text))
```

### 优先级总结

| 数据源 | 优先级 | 与其他数据源关系 |
|--------|--------|------------------|
| Answerers | 最高 | 独立，始终执行 |
| 内部语法补全 | 中 | 与外部后端互斥 |
| 外部搜索后端 | 最低 | 与内部语法补全互斥 |

---

## 三、去重规则

### 1. 局部去重（存在）

**a. 内部语法补全的去重**

在 [query.py#L38-L40](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L38-L40) 的 `_add_autocomplete` 方法中：
```python
def _add_autocomplete(self, value):
    if value not in self.raw_text_query.autocomplete_list:
        self.raw_text_query.autocomplete_list.append(value)
```
- 确保 `autocomplete_list` 内部无重复项

**b. 外部搜索结果的过滤**

在 [webapp.py#L840](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py#L840) 中：
```python
if result != sug_prefix:
```
- 排除与用户输入完全相同的建议

### 2. 全局去重（不存在）

**重要发现**：代码中**没有**对最终的 `results` 列表进行全局去重。

这意味着：
- Answerers 结果与其他数据源的结果可能重复
- 如果多个 answerer 返回相同答案，也会重复出现
- 最终返回给前端的列表可能包含重复项

### 3. AnswerSet 的去重机制（用于搜索结果页，非自动补全）

虽然自动补全接口没有全局去重，但 [answer.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/result_types/answer.py) 中定义的 `AnswerSet` 类有完整的去重逻辑（基于 hash），但该类用于普通搜索结果页的答案展示，**不用于** `/autocompleter` 接口。

---

## 四、完整调用链

```
用户输入查询
    ↓
POST/GET /autocompleter
    ↓
RawTextQuery 初始化，解析查询语法
    ├─ 生成 autocomplete_list（内部语法建议）
    └─ 提取 sug_prefix（实际查询内容）
    ↓
调用 Answerers.STORAGE.ask(sug_prefix)
    → 添加 Answer 类型结果到 results
    ↓
判断 autocomplete_list 是否为空
    ├─ 为空 → 调用 search_autocomplete(backend_name, sug_prefix, sxng_locale)
    │       → 过滤 sug_prefix 本身
    │       → 转换为完整查询后添加到 results
    └─ 非空 → 添加 autocomplete_list 中的建议到 results
    ↓
根据请求来源格式化输出
    ├─ AJAX 请求 → 返回 JSON 数组
    └─ 浏览器地址栏 → 返回 OpenSearch 格式 [query, suggestions]
    ↓
返回响应
```

---

## 五、配置相关

### 用户配置

在 [preferences.py#L420-L423](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/preferences.py#L420-L423) 中定义：
```python
'autocomplete': EnumStringSetting(
    settings['search']['autocomplete'],
    locked=is_locked('autocomplete'),
    choices=list(autocomplete.backends.keys()) + ['']
)
```
- 用户可从17个后端中选择一个，或选择空字符串禁用
- 管理员可通过 `locked` 配置锁定该选项

### 系统默认配置

在 [settings.yml#L46-L48](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/settings.yml#L46-L48) 中：
```yaml
autocomplete: ""
autocomplete_min: 4
```
- 默认禁用自动补全
- 最少输入4个字符才触发自动补全

---

## 六、关键代码文件总结

| 文件 | 作用 |
|------|------|
| [searx/webapp.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py) | 自动补全路由入口，合并逻辑 |
| [searx/autocomplete.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/autocomplete.py) | 17个外部后端实现 |
| [searx/query.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py) | 内部语法解析与补全 |
| [searx/external_bang.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py) | 外部 bang 的 Trie 检索、BFS 排序与去重 |
| [searx/answerers/_core.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/answerers/_core.py) | Answerers 核心框架 |
| [searx/preferences.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/preferences.py) | 用户偏好设置 |

---

## 七、潜在问题与改进空间

1. **缺少全局去重**：最终结果可能包含重复项，建议在返回前添加 `results = list(dict.fromkeys(results))` 来去重

2. **外部后端单一**：每次只能选择一个外部后端，无法同时从多个搜索引擎获取建议后合并

3. **互斥设计可能不符合用户预期**：当用户输入 `!python` 时，只能看到引擎补全，无法看到关于 "python" 的搜索建议

---

## 附录 A：查询片段混排时的补全锁定机制

当特殊查询片段（如 `!google`、`:en`、`!!g`、`<3`）与普通搜索词混合出现时，补全**锁定哪个片段**由 `_parse_query()` 方法精确控制。此机制是理解混排行为的关键。

### 核心算法

锁定逻辑位于 [query.py#L280-L309](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L280-L309)：

```python
def _parse_query(self):
    raw_query_parts = re.split(r'(\s+)', self.query)  # 保留空白字符分割

    last_index_location = None
    autocomplete_index = len(raw_query_parts) - 1     # ★ 锁定「最后一个非空片段」

    for i, query_part in enumerate(raw_query_parts):
        if query_part.isspace() or query_part == '':
            continue

        special_part = False
        for parser_class in RawTextQuery.PARSER_CLASSES:
            if parser_class.check(query_part):
                # ★ 只有当 i == autocomplete_index（即最后一个片段）时，才启用自动补全
                special_part = parser_class(self, i == autocomplete_index)(query_part)
                break

        qlist = self.query_parts if special_part else self.user_query_parts
        qlist.append(query_part)
        last_index_location = (qlist, len(qlist) - 1)  # ★ 始终记录「最后一个片段」

    self.autocomplete_location = last_index_location  # ★ 最终锁定位置
```

### 锁定规则（逐条精确说明）

| 规则 | 说明 |
|------|------|
| **锁定目标** | 始终锁定查询字符串中的**最后一个非空片段** |
| **补全开关** | 解析器构造函数的第二个参数 `enable_autocomplete = (i == autocomplete_index)`，即只有最后一个片段才开启补全生成 |
| **位置存储** | `autocomplete_location = (所属列表引用, 列表内索引)`，其中所属列表为 `query_parts`（特殊语法）或 `user_query_parts`（普通搜索词） |
| **补全替换** | `get_autocomplete_full_query(text)` 会直接用 `text` 替换 `autocomplete_location` 指向的位置，然后重新拼接完整查询 |

### 混排示例（对照测试用例）

#### 示例 1：最后一个是特殊语法 → 补全特殊语法，不调用外部后端

输入：`"the query !dum"`

| 步骤 | 结果 |
|------|------|
| 分割后片段 | `["the", " ", "query", " ", "!dum"]` |
| `autocomplete_index` | `4`（即 `!dum`） |
| `!dum` 的 `enable_autocomplete` | `True`（是最后一个） |
| `BangParser._autocomplete` 生成 | `["!dummy_engine", "!dummy_private_engine"]` |
| `autocomplete_location` | `(query_parts, 0)` |
| `get_autocomplete_full_query("!dummy_engine")` | `!dummy_engine the query`（提升到最前面） |
| 是否调用外部后端 | ❌ 否（`autocomplete_list` 非空） |

参考测试：[test_query.py#L232-L234](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_query.py#L232-L234)

#### 示例 2：特殊语法在前，普通词在后 → 补全普通词，调用外部后端

输入：`"!dum the query"`

| 步骤 | 结果 |
|------|------|
| 分割后片段 | `["!dum", " ", "the", " ", "query"]` |
| `autocomplete_index` | `4`（即 `query`） |
| `!dum` 的 `enable_autocomplete` | `False`（不是最后一个） |
| `BangParser._autocomplete` 生成 | `[]`（补全开关关闭） |
| `autocomplete_list` | `[]` |
| `sug_prefix` | `!dum the query`（`!dum` 未匹配任何引擎名，所以保留在 user_query_parts 中） |
| 是否调用外部后端 | ✅ 是（`autocomplete_list` 为空） |

参考测试：[test_query.py#L236-L238](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_query.py#L236-L238)

#### 示例 3：外部 bang 在末尾 → 补全外部 bang

输入：`"the query !!dd"`

| 步骤 | 结果 |
|------|------|
| 最后一个片段 | `!!dd` |
| `ExternalBangParser` 启用补全，生成 | 若干 `!!xxx` 建议 |
| `get_autocomplete_full_query("!!ddg")` | `!!ddg the query`（提升到最前面） |
| 是否调用外部后端 | ❌ 否 |

参考测试：[test_query.py#L193-L203](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_query.py#L193-L203)

#### 示例 4：语言标签在中间，普通词在末尾 → 补全普通词

输入：`":en python tuto"`

| 步骤 | 结果 |
|------|------|
| 最后一个片段 | `tuto` |
| `:en` 和 `python` 的补全开关 | 关闭 |
| `autocomplete_list` | `[]` |
| `sug_prefix` | `python tuto`（`:en` 被识别为特殊语法放入 `query_parts`，从 `getQuery()` 中排除） |
| 是否调用外部后端 | ✅ 是（对 `python tuto` 进行补全） |

### 「位置提升」现象

`getFullQuery()` 的拼接公式为：
```python
return '{0} {1}'.format(' '.join(self.query_parts), self.getQuery()).strip()
#                  ↑ 特殊语法在前                ↑ 普通搜索词在后
```

因此无论用户把特殊语法片段输入在什么位置，当补全建议替换最后一个片段后，**所有 `query_parts` 都会出现在最终查询的最前面**，而 `user_query_parts` 则紧随其后。这就是测试中 `"the query !dum"` 的补全结果是 `"!dummy_engine the query"`（`!dummy_engine` 被提升到前面）的原因。

### ★ 核心决策：内部语法补全 vs 外部搜索建议

`webapp.py` 路由层的判断非常简单（[webapp.py#L831](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py#L831)）：

```python
if len(raw_text_query.autocomplete_list) == 0 and len(sug_prefix) > 0:
    # → 调用外部搜索后端（search_autocomplete）
    ...
if len(raw_text_query.autocomplete_list) > 0:
    # → 使用内部语法补全
    ...
```

也就是说，**切换的唯一依据是 `autocomplete_list` 是否为空**，而 `autocomplete_list` 是否为空由以下两个条件的**逻辑与**决定：
1. 最后一个非空片段**匹配了某解析器**的 `check()` 条件
2. 该解析器的**补全触发条件**被满足（每个解析器的条件差异巨大）

**各解析器补全触发条件的精确差异**（必须 `enable_autocomplete=True` 即「是最后一个片段」为前提）：

| 解析器 | check 条件 | 补全触发条件（__call__ 内部） | 代码位置 | found=True 时补全吗？ | found=False 时补全吗？ |
|--------|-----------|------------------------------|---------|----------------------|----------------------|
| **TimeoutParser** | 首字符为 `<` | `if enable_autocomplete **and not value**` | [query.py#L51](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L51) | 不相关，只看 value 是否为空 |
| **LanguageParser** | 首字符为 `:` | `if enable_autocomplete **and not found**` | [query.py#L80](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L80) | ❌ **不补全** | ✅ 补全 |
| **ExternalBangParser** | 以 `!!` 开头且长度>2 | `if enable_autocomplete`（**无条件**） | [query.py#L159](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L159) | ✅ 补全（即使 bang 完全有效） | ✅ 补全 |
| **BangParser** | 首字符为 `!` 且不是 `!!` | `if enable_autocomplete`（**无条件**） | [query.py#L189](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L189) | ✅ 补全（即使引擎名完全有效） | ✅ 补全 |
| **FeelingLuckyParser** | 恰好等于 `!!` | 无补全逻辑 | [query.py#L240-L247](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L240-L247) | —（无补全） | —（无补全） |

⚠️ **关键反例（极易出错的场景）**：

| 输入 | 最后片段 | 解析器 | 补全触发条件结果 | autocomplete_list | 最终行为 |
|------|---------|--------|---------------|-------------------|---------|
| `"hello :en"` | `:en` | LanguageParser | found=True（en 有效）→ **not found = False** | `[]`（空！） | ✅ **调用外部后端**（对 `"hello"` 进行补全，`:en` 被作为特殊语法排除） |
| `"hello :xyz"` | `:xyz` | LanguageParser | found=False（xyz 无效）→ **not found = True** | 非空 | ❌ 用内部语法补全 |
| `"hello <3"` | `<3` | TimeoutParser | value = "3" → **not value = False** | `[]`（空！） | ✅ **调用外部后端**（对 `"hello"` 进行补全，`<3` 被解析为超时） |
| `"hello <"` | `<` | TimeoutParser | value = "" → **not value = True** | 非空 | ❌ 用内部语法补全 |
| `"hello <abc"` | `<abc` | TimeoutParser | value = "abc" → **not value = False**，且 found=False（abc 非数字）→ **special_part=False** | `[]` 且 `<abc` 留在 user_query_parts | ✅ **调用外部后端**（对 `"hello <abc"` 整体补全） |
| `"hello !google"` | `!google` | BangParser | 无条件补全 | 非空 | ❌ 用内部语法补全（即使 `google` 是有效引擎名） |
| `"hello !!"` | `!!` | FeelingLuckyParser | 无补全逻辑 | `[]` | ✅ **调用外部后端**（同时启用 I'm Feeling Lucky 跳转） |

### 补全行为速查表（精确版）

| 最后一个片段 | 具体情况 | `enable_autocomplete` | `autocomplete_list` | 是否调用外部后端 |
|-------------|---------|----------------------|--------------------|----------------|
| **普通搜索词**（无前缀） | 任意 | N/A（不进入解析器） | ❌ 空 | ✅ |
| **`<`** 单独一个字符 | value="" | ✅ | ✅ 非空（`<3`, `<850`） | ❌ |
| **`<有效数字>`**（如 `<3`, `<850`） | value 非空且全数字 | ✅ | ❌ 空（解析成功但不触发补全） | ✅（去掉超时设置后补全剩余词） |
| **`<非数字>`**（如 `<abc`） | value 非空且含非数字字符 | ✅ | ❌ 空（special_part=False，留在普通词里） | ✅（连同 `<abc` 一起作为普通搜索词补全） |
| **`:` 单独一个字符** | value="" → found=False | ✅ | ✅ 非空（示例语言代码） | ❌ |
| **`:有效语言码`**（如 `:en`, `:zh-CN`） | found=True | ✅ | ❌ 空（解析成功不触发补全） | ✅（去掉语言标签后补全剩余词） |
| **`:无效/部分语言`**（如 `:englis`, `:xyz`） | found=False | ✅ | ✅ 非空（匹配的语言建议） | ❌ |
| **`!!任意字符`**（长度≥3，如 `!!g`, `!!dd`） | 无条件补全 | ✅ | ✅ 非空（空时给默认 `g/ddg/bing`） | ❌ |
| **`!!` 恰好两个** | 进入 FeelingLuckyParser，无补全 | ✅ | ❌ 空 | ✅（同时启用首结果跳转） |
| **`!` 单独一个字符** | 无条件补全 | ✅ | ✅ 非空（默认 `images/wikipedia/osm`） | ❌ |
| **`!任意字符`**（如 `!g`, `!goo`, `!google`） | 无条件补全 | ✅ | ✅ 非空（匹配的引擎/分类/快捷方式） | ❌（即使是完全有效的引擎名也一样） |

---

## 附录 B：外部 bang 建议的排序与去重规则

外部 bang（`!!` 前缀）的建议生成逻辑最为复杂，涉及 **Trie 字典树遍历**、**BFS 广度优先搜索**、**两级去重**和**权重排序**。核心实现位于 [external_bang.py#L64-L90](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L64-L90)。

### 数据结构基础

外部 bang 数据库以 **Trie（前缀树）** 结构存储，`LEAF_KEY = chr(16)` 标记某个前缀是完整可跳转的 bang。

例如 `TEST_DB` 示例：
```python
'trie': {
    'exam': {
        'ple': '//example.com/...\x010',   # rank=0
        LEAF_KEY: '//wikipedia.org/...\x010',  # "exam" 本身也是完整 bang
    },
    'sea': {
        LEAF_KEY: 'sea...\x010',               # "sea" 是完整 bang，rank=0
        'rch': {
            LEAF_KEY: 'search...\x010',        # "search" 是完整 bang
            'ing': 'searching...\x010',        # "searching" 是完整 bang
        },
        's': {
            'on': 'season...\x010',            # "season" 是完整 bang
            'capes': 'seascape...\x010',       # "seascape" 是完整 bang
        },
    },
}
```

### 两级处理流程

#### 第一级：`get_bang_definition_and_ac()` — 直接子节点匹配

位置：[external_bang.py#L30-L46](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L30-L46)

```python
def get_bang_definition_and_ac(external_bangs_db, bang):
    node, before, after = get_node(external_bangs_db, bang)

    if after != '':
        # 部分匹配：只返回同层以 after 开头的兄弟键
        for k in node:
            if k.startswith(after):
                bang_ac_list.append(before + k)
    elif isinstance(node, dict):
        # 精确匹配节点：返回所有子键（排除 LEAF_KEY）
        bang_definition = node.get(LEAF_KEY)
        bang_ac_list = [before + k for k in node.keys() if k != LEAF_KEY]
    elif isinstance(node, str):
        # 精确匹配叶子：没有子节点
        bang_definition = node
        bang_ac_list = []
```

⚠️ **局限**：此函数只返回**直接子节点/兄弟节点**，不会递归深入。例如输入 `sea` 时只能得到 `['sear', 'ses']`（即 `sea` 的一级子键 `rch` 和 `s` 分别组合为 `sear`、`ses`），不会得到 `'search'`、`'season'` 等更深层的完整 bang。

#### 第二级：`get_bang_definition_and_autocomplete()` — BFS 递归展开 + 排序

位置：[external_bang.py#L64-L90](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L64-L90)

这是真正供 `ExternalBangParser` 调用的**最终接口**，完整流程如下：

```
步骤 1：调用 get_bang_definition_and_ac() 获取第一层候选 bang_ac_list
  ↓
步骤 2：将 bang_ac_list 放入 BFS 队列 current
  ↓
步骤 3：BFS 展开所有可达节点
  循环 while current 非空：
    ├─ bang_ac = current.pop(0)          （FIFO 出队）
    ├─ done.add(bang_ac)                 （去重①标记已处理）
    ├─ 调用 get_bang_definition_and_ac(bang_ac) 获取该节点定义和子节点
    ├─ 如果有 LEAF 定义：
    │     解析 rank（从 bang_definition 用 \x01 分隔）
    │     将 (bang_ac, rank) 加入 new_autocomplete
    └─ 遍历当前节点的所有子候选 new_bang：
          若 new_bang ∉ done 且 new_bang ∉ current （去重②）
            → current.append(new_bang)
  ↓
步骤 4：按 key=(-rank, bang名称) 排序 new_autocomplete
  ↓
步骤 5：只保留 bang 名称，返回列表
```

### 去重机制详解

外部 bang 使用**双保险去重**：

| 去重层级 | 实现代码 | 数据结构 | 检查时机 | 作用 |
|----------|----------|----------|----------|------|
| 去重① | `done.add(bang_ac)` | `set()` | 节点出队后立即标记 | 防止同一个 bang 被**重复处理**（避免重复计算 rank 和子节点） |
| 去重② | `if new_bang not in done and new_bang not in current` | `set()` + `list` 队列 | 子节点入队前检查 | 防止同一个节点被**多次加入等待队列**（避免队列膨胀、重复入队） |

为什么需要两层？因为 Trie 树中理论上可能存在不同路径汇聚到同一字符串的情况（虽然实际数据中这种情况极少，但算法做了防御性处理）。参考 [external_bang.py#L74-L85](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L74-L85)：

```python
done = set()                         # 去重①：已处理过的 bang
current = [*bang_ac_list]            # BFS 队列初始值
while len(current) > 0:
    bang_ac = current.pop(0)         # BFS，FIFO
    done.add(bang_ac)                # ★ 标记为已处理（出队即锁定）
    ...
    for new_bang in current_bang_ac_list:
        if new_bang not in done and new_bang not in current:  # ★ 入队前双重检查
            current.append(new_bang)
```

### 排序规则详解

排序位于 [external_bang.py#L87-L88](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L87-L88)：

```python
new_autocomplete.sort(key=lambda t: (-t[1], t[0]))
new_autocomplete = list(map(lambda t: t[0], new_autocomplete))
```

排序键为**二元组** `(-rank, bang名称)`，含义如下：

| 排序维度 | 规则 | 说明 |
|----------|------|------|
| **第一关键字 `rank`** | **降序**（取负号 `-rank` 实现） | `rank` 越大（流行度/优先级越高）越靠前；`rank` 来自 bang 定义中 `\x01` 分隔符后的数字，如 `...\x0142` 表示 rank=42 |
| **第二关键字 `bang名称`** | **字典序升序** | rank 相同时，按 bang 字符串的字母顺序排列；保证结果在 rank 相同时稳定可预测 |

`rank` 的解析来自 [resolve_bang_definition()](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L49-L61)：
```python
def resolve_bang_definition(bang_definition, query):
    url, rank = bang_definition.split(chr(1))  # \x01 是 URL 与 rank 的分隔符
    rank = int(rank) if len(rank) > 0 else 0
    ...
```

### 完整示例追踪（对照测试）

输入：`sea`，使用 `TEST_DB`

**初始状态**：
- 调用 `get_bang_definition_and_ac('sea')` → `bang_definition = sea...`（`sea` 本身有 LEAF），`bang_ac_list = ['sear', 'ses']`（子键 `rch` + `s` 与前缀 `sea` 拼接）
- `current = ['sear', 'ses']`，`done = set()`，`new_autocomplete = [('sea', 0)]`（`sea` 本身有定义，在 BFS 前就加入）

**BFS 循环**：

| 轮次 | 出队元素 | done 新增 | 该 bang 有定义？ | rank | 加入 new_autocomplete | 子节点 | 入队检查 | current 新状态 |
|------|----------|-----------|----------------|------|---------------------|--------|---------|---------------|
| 1 | `sear` | `sear` | ✅ `search`（LEAF） | 0 | `('search', 0)` | `['searching']` | 不在 done/current → ✅ | `['ses', 'searching']` |
| 2 | `ses` | `ses` | ❌ 无直接定义，但子节点 `on`、`capes` → 展开后 `season`(rank0)、`seascape`(rank0) 分别有定义 | 0 | `('season', 0)`, `('seascape', 0)` | `['season', 'seascape']` 已是完整 bang 且已加入结果，但它们的子节点继续入队… | 实际 TEST_DB 中 `season`、`seascape` 均为叶子无子节点，所以无新入队 | `['searching']` |
| 3 | `searching` | `searching` | ✅ `searching` 有定义 | 0 | `('searching', 0)` | `[]`（无子节点） | - | `[]` |

**最终 `new_autocomplete`**（带 rank 的原始列表）：
```python
[('sea', 0), ('search', 0), ('season', 0), ('seascape', 0), ('searching', 0)]
```

排序（所有 rank=0，按字母序）：`sea < seascape < search < searching < season`

但注意 `sea` 本身虽然有定义，`ExternalBangParser._parse()` 在找到 `bang_definition` 后，**最终输出的 `new_autocomplete` 不包含输入本身**（因为 `search` 和 `sea` 是 LEAF 节点，且 `get_bang_definition_and_autocomplete` 的 BFS 处理的是子节点）。根据 [test_external_bangs.py#L98-L101](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_external_bangs.py#L98-L101) 的断言，期望输出为：

**最终输出**：`['search', 'searching', 'seascapes', 'season']` ✅ 与测试一致。

（注：实际 TEST_DB 中 `sea.s.capes` 对应的完整 bang 是 `seascapes`，因为路径为 `sea` → `s` → `capes`，拼接为 `seas` + `capes` = `seascapes`）

### 空输入的回退逻辑 + 第三层去重

在 [query.py#L171-L175](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L171-L175)，外部 bang 补全的最后一步还有**回退逻辑**和**第三层去重**：

```python
def _autocomplete(self, bang_ac_list):
    if not bang_ac_list:
        # ★ 回退：当 Trie 中找不到任何匹配时，使用硬编码的 3 个热门 bang
        bang_ac_list = ['g', 'ddg', 'bing']

    for external_bang in bang_ac_list:
        # ★ 第三层去重（调用公共 _add_autocomplete 方法）
        #    确保不会与其他解析器生成的结果重复
        self._add_autocomplete('!!' + external_bang)
```

| 层级 | 去重机制 | 作用域 |
|------|---------|--------|
| 第 1 层 | `done.add(bang_ac)`（BFS 出队标记） | external_bang.py 内部 BFS 展开过程 |
| 第 2 层 | `if new_bang not in done and new_bang not in current`（入队前检查） | external_bang.py 内部 BFS 队列 |
| 第 3 层 | `_add_autocomplete()` 中的 `if value not in autocomplete_list` | query.py 层面，确保跨解析器不重复 |
