# SearXNG 后端自动补全功能分析

## 概述

SearXNG 的自动补全功能从**三类独立数据源**获取建议，按照特定优先级规则进行合并，并在局部进行去重处理。整个流程的核心入口位于 [webapp.py#L811-L857](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py#L811-L857) 的 `autocompleter()` 路由函数。

---

## 一、多数据源获取机制

自动补全功能从以下三个独立数据源获取建议：

### 1. Answerers（内部即时回答系统）

**位置**：[searx/answerers/_core.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/answerers/_core.py)

**作用**：提供即时答案类的补全建议（如数学计算、统计数据、随机数等）。

**调用方式**：
```python
for obj in searx.answerers.STORAGE.ask(sug_prefix):
    if isinstance(obj, Answer):
        results.append(obj.answer)
```

**工作原理**：
- `AnswerStorage.ask()` 方法检查查询的第一个关键词是否匹配已注册 answerer 的关键词
- 如果匹配，则调用对应 answerer 的 `answer()` 方法获取结果
- 内置 answerer 包括：`random`（随机数生成）、`statistics`（统计信息）等
- 每次请求**都会**调用此数据源

### 2. 内部语法解析器自动补全

**位置**：[searx/query.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py)

**作用**：为 SearXNG 特有的查询语法提供自动补全。

**包含的解析器**：

| 解析器 | 触发前缀 | 补全内容 | 位置 |
|--------|----------|----------|------|
| `TimeoutParser` | `<` | 超时设置，如 `<3`（3秒）、`<850`（850毫秒） | [query.py#L43-L69](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L43-L69) |
| `LanguageParser` | `:` | 语言代码，如 `:en`、`:zh`、`:english` | [query.py#L72-L149](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L72-L149) |
| `ExternalBangParser` | `!!` | 外部 bang 命令，如 `!!g`、`!!ddg` | [query.py#L151-L175](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L151-L175) |
| `BangParser` | `!` | 引擎/分类名称，如 `!google`、`!images` | [query.py#L178-L237](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L178-L237) |

**工作原理**：
- `RawTextQuery` 初始化时自动调用各解析器
- 解析器识别查询中的特殊语法前缀（如 `!`, `:`, `<`, `!!`）
- 如果前缀匹配且启用了自动补全，则生成相关建议并存储在 `autocomplete_list` 中

### 3. 外部搜索后端

**位置**：[searx/autocomplete.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/autocomplete.py)

**作用**：从第三方搜索引擎获取搜索建议。

**可用后端**（共17个）：

| 后端名称 | 实现函数 | 数据源 |
|----------|----------|--------|
| `360search` | `qihu360search()` | 360搜索 |
| `baidu` | `baidu()` | 百度 |
| `bing` | `bing()` | 必应 |
| `brave` | `brave()` | Brave搜索 |
| `dbpedia` | `dbpedia()` | DBpedia |
| `duckduckgo` | `duckduckgo()` | DuckDuckGo |
| `google` | `google_complete()` | Google |
| `mwmbl` | `mwmbl()` | Mwmbl |
| `naver` | `naver()` | Naver |
| `quark` | `quark()` | 夸克 |
| `qwant` | `qwant()` | Qwant |
| `seznam` | `seznam()` | Seznam |
| `sogou` | `sogou()` | 搜狗 |
| `startpage` | `startpage()` | Startpage |
| `swisscows` | `swisscows()` | Swisscows |
| `wikipedia` | `wikipedia()` | 维基百科 |
| `yandex` | `yandex()` | Yandex |

**后端注册表**：
```python
backends: dict[str, t.Callable[[str, str], list[str]]] = {
    '360search': qihu360search,
    'baidu': baidu,
    'bing': bing,
    # ... 共17个后端
}
```

**调用方式**：
```python
def search_autocomplete(backend_name: str, query: str, sxng_locale: str) -> list[str]:
    backend = backends.get(backend_name)
    if backend is None:
        return []
    try:
        return backend(query, sxng_locale)
    except (HTTPError, SearxEngineResponseException):
        return []
```

**重要特点**：
- 用户在偏好设置中**只能选择一个**后端
- 支持多语言（部分后端如 `google_complete`、`duckduckgo`、`wikipedia` 会根据 `sxng_locale` 参数调整请求）
- 网络异常时静默失败，返回空列表

---

## 二、合并规则

### 核心逻辑

合并逻辑在 [webapp.py#L811-L857](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py#L811-L857) 中实现，遵循以下**优先级顺序**和**互斥规则**：

```
执行流程：
├─ 1. 收集 Answerers 结果（无条件执行）
├─ 2. 检查是否有内部语法补全结果 (autocomplete_list)
│   ├─ 有：添加内部语法补全结果（不调用外部后端）
│   └─ 无：调用外部搜索后端并添加结果
└─ 3. 返回最终结果
```

### 详细合并步骤

**步骤1：添加 Answerers 结果**（第825-827行）
```python
for obj in searx.answerers.STORAGE.ask(sug_prefix):
    if isinstance(obj, Answer):
        results.append(obj.answer)
```
- 每次请求都会执行
- 结果最先被添加到 `results` 列表

**步骤2：判断是否调用外部后端**（第831行）
```python
if len(raw_text_query.autocomplete_list) == 0 and len(sug_prefix) > 0:
```
- **关键互斥条件**：只有当 `autocomplete_list` 为空且查询非空时，才调用外部搜索后端
- ⚠️ **重要澄清**：**不是**「有特殊语法前缀就不调用外部后端」。即使输入了 `!`/`:`/`<` 前缀，如果该解析器不触发补全（如 `:en` 合法语言、`<3` 合法超时），`autocomplete_list` 仍然为空，**仍然会调用外部后端**。详见附录 A 的反例。

**步骤3：添加外部搜索后端结果**（第837-841行）
```python
for result in search_autocomplete(backend_name, sug_prefix, sxng_locale):
    if result != sug_prefix:
        results.append(raw_text_query.changeQuery(result).getFullQuery())
```

**步骤4：添加内部语法补全结果**（第843-845行）
```python
if len(raw_text_query.autocomplete_list) > 0:
    for autocomplete_text in raw_text_query.autocomplete_list:
        results.append(raw_text_query.get_autocomplete_full_query(autocomplete_text))
```

### 优先级总结

| 数据源 | 优先级 | 与其他数据源关系 |
|--------|--------|------------------|
| Answerers | 最高 | 独立，始终执行 |
| 内部语法补全 | 中 | 与外部后端互斥 |
| 外部搜索后端 | 最低 | 与内部语法补全互斥 |

---

## 三、去重规则

### 1. 局部去重（存在）

**a. 内部语法补全的去重**

在 [query.py#L38-L40](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L38-L40) 的 `_add_autocomplete` 方法中：
```python
def _add_autocomplete(self, value):
    if value not in self.raw_text_query.autocomplete_list:
        self.raw_text_query.autocomplete_list.append(value)
```
- 确保 `autocomplete_list` 内部无重复项

**b. 外部搜索结果的过滤**

在 [webapp.py#L840](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py#L840) 中：
```python
if result != sug_prefix:
```
- 排除与用户输入完全相同的建议

### 2. 全局去重（不存在）

**重要发现**：代码中**没有**对最终的 `results` 列表进行全局去重。

这意味着：
- Answerers 结果与其他数据源的结果可能重复
- 如果多个 answerer 返回相同答案，也会重复出现
- 最终返回给前端的列表可能包含重复项

### 3. AnswerSet 的去重机制（用于搜索结果页，非自动补全）

虽然自动补全接口没有全局去重，但 [answer.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/result_types/answer.py) 中定义的 `AnswerSet` 类有完整的去重逻辑（基于 hash），但该类用于普通搜索结果页的答案展示，**不用于** `/autocompleter` 接口。

---

## 四、完整调用链

```
用户输入查询
    ↓
POST/GET /autocompleter
    ↓
RawTextQuery 初始化，解析查询语法
    ├─ 生成 autocomplete_list（内部语法建议）
    └─ 提取 sug_prefix（实际查询内容）
    ↓
调用 Answerers.STORAGE.ask(sug_prefix)
    → 添加 Answer 类型结果到 results
    ↓
判断 autocomplete_list 是否为空
    ├─ 为空 → 调用 search_autocomplete(backend_name, sug_prefix, sxng_locale)
    │       → 过滤 sug_prefix 本身
    │       → 转换为完整查询后添加到 results
    └─ 非空 → 添加 autocomplete_list 中的建议到 results
    ↓
根据请求来源格式化输出
    ├─ AJAX 请求 → 返回 JSON 数组
    └─ 浏览器地址栏 → 返回 OpenSearch 格式 [query, suggestions]
    ↓
返回响应
```

---

## 五、配置相关

### 用户配置

在 [preferences.py#L420-L423](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/preferences.py#L420-L423) 中定义：
```python
'autocomplete': EnumStringSetting(
    settings['search']['autocomplete'],
    locked=is_locked('autocomplete'),
    choices=list(autocomplete.backends.keys()) + ['']
)
```
- 用户可从17个后端中选择一个，或选择空字符串禁用
- 管理员可通过 `locked` 配置锁定该选项

### 系统默认配置

在 [settings.yml#L46-L48](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/settings.yml#L46-L48) 中：
```yaml
autocomplete: ""
autocomplete_min: 4
```
- 默认禁用自动补全
- 最少输入4个字符才触发自动补全

---

## 六、关键代码文件总结

| 文件 | 作用 |
|------|------|
| [searx/webapp.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py) | 自动补全路由入口，合并逻辑 |
| [searx/autocomplete.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/autocomplete.py) | 17个外部后端实现 |
| [searx/query.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py) | 内部语法解析与补全 |
| [searx/external_bang.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py) | 外部 bang 的 Trie 检索、BFS 排序与去重 |
| [searx/answerers/_core.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/answerers/_core.py) | Answerers 核心框架 |
| [searx/preferences.py](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/preferences.py) | 用户偏好设置 |

---

## 七、潜在问题与改进空间

1. **缺少全局去重**：最终结果可能包含重复项，建议在返回前添加 `results = list(dict.fromkeys(results))` 来去重

2. **外部后端单一**：每次只能选择一个外部后端，无法同时从多个搜索引擎获取建议后合并

3. **互斥设计可能不符合用户预期**：当用户输入 `!python` 时，只能看到引擎补全，无法看到关于 "python" 的搜索建议

---

## 附录 A：查询片段混排时的补全锁定机制

当特殊查询片段（如 `!google`、`:en`、`!!g`、`<3`）与普通搜索词混合出现时，补全**锁定哪个片段**由 `_parse_query()` 方法精确控制。此机制是理解混排行为的关键。

### 核心算法

锁定逻辑位于 [query.py#L280-L309](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L280-L309)：

```python
def _parse_query(self):
    raw_query_parts = re.split(r'(\s+)', self.query)  # 保留空白字符分割

    last_index_location = None
    autocomplete_index = len(raw_query_parts) - 1     # ★ 锁定「最后一个非空片段」

    for i, query_part in enumerate(raw_query_parts):
        if query_part.isspace() or query_part == '':
            continue

        special_part = False
        for parser_class in RawTextQuery.PARSER_CLASSES:
            if parser_class.check(query_part):
                # ★ 只有当 i == autocomplete_index（即最后一个片段）时，才启用自动补全
                special_part = parser_class(self, i == autocomplete_index)(query_part)
                break

        qlist = self.query_parts if special_part else self.user_query_parts
        qlist.append(query_part)
        last_index_location = (qlist, len(qlist) - 1)  # ★ 始终记录「最后一个片段」

    self.autocomplete_location = last_index_location  # ★ 最终锁定位置
```

### 锁定规则（逐条精确说明）

| 规则 | 说明 |
|------|------|
| **锁定目标** | 始终锁定查询字符串中的**最后一个非空片段** |
| **补全开关** | 解析器构造函数的第二个参数 `enable_autocomplete = (i == autocomplete_index)`，即只有最后一个片段才开启补全生成 |
| **位置存储** | `autocomplete_location = (所属列表引用, 列表内索引)`，其中所属列表为 `query_parts`（特殊语法）或 `user_query_parts`（普通搜索词） |
| **补全替换** | `get_autocomplete_full_query(text)` 会直接用 `text` 替换 `autocomplete_location` 指向的位置，然后重新拼接完整查询 |

### ★ 前置：sug_prefix 的精确构成

`webapp.py` L823：`sug_prefix = raw_text_query.getQuery()`，而 `getQuery()` 的定义是（[query.py#L323-L324](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L323-L324)）：
```python
def getQuery(self):
    return ' '.join(self.user_query_parts)
```

因此，**sug_prefix 完全等于 `user_query_parts` 的空格拼接**。而一个片段进入 `query_parts` 还是 `user_query_parts`，完全由 `_parse_query()` 中 `special_part` 的布尔值决定（[query.py#L308](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L308)）：

```python
special_part = parser_class(self, enable_autocomplete)(query_part)
qlist = self.query_parts if special_part else self.user_query_parts
qlist.append(query_part)
```

即：**`special_part` = 解析器 `__call__` 的返回值 `found`**。

`special_part`（found）与「是否触发补全」是两个**完全独立**的维度：

| 情形 | 解析器 found | special_part | 片段归属 | 是否进入 sug_prefix | 是否触发补全 |
|------|------------|-------------|---------|-------------------|------------|
| `<3` 是最后一个 | True | True | `query_parts` | ❌ **排除** | ❌（value 非空，不触发） |
| `:en` 是最后一个 | True | True | `query_parts` | ❌ **排除** | ❌（found=True，不触发） |
| `<abc` 是最后一个 | False | False | `user_query_parts` | ✅ **保留** | ❌（value 非空，不触发） |
| `:xyz` 是最后一个 | False | False | `user_query_parts` | ✅ **保留** | ✅（found=False 且 enable_autocomplete，触发） |
| `!dum` 是最后一个 | 取决于 dum 是否有效引擎 | 有效→True/无效→False | 有效→`query_parts`/无效→`user_query_parts` | 有效❌/无效✅ | ✅（BangParser 无条件补全） |
| `!!gg` 是最后一个 | 取决于 gg 是否有效 bang | 有效→True/无效→False | 有效→`query_parts`/无效→`user_query_parts` | 有效❌/无效✅ | ✅（ExternalBangParser 无条件补全） |

⚠️ **这正是文档中之前混淆的核心**：外部搜索 sug_prefix 排除哪些片段，与「是否触发内部补全」无关，**只与 found（special_part）直接相关**。

---

### 混排示例（对照测试用例，逐片段追踪）

#### 示例 1：最后一个是 `!dum`（无效引擎名）→ 补全内部语法，不调用外部后端

输入：`"the query !dum"`，**逐片段追踪**：

| 片段 | 是否进入解析器 | enable_autocomplete | 解析器 found（=special_part） | 片段归属 | `getQuery()` 包含？ |
|------|--------------|-------------------|---------------------------|---------|-------------------|
| `the` | 否（无前缀） | N/A | False | `user_query_parts` | ✅ |
| `query` | 否（无前缀） | N/A | False | `user_query_parts` | ✅ |
| `!dum` | 是（BangParser） | **True**（是最后一个） | **False**（dum 不是有效引擎/分类） | ❗ `user_query_parts`（虽然有 `!` 前缀，但 found=False） | ✅ |
| 汇总 | | | | | `sug_prefix = "the query !dum"` |

补全相关：
- `BangParser._autocomplete('!', 'dum')`：遍历 categories/engines/shortcuts 找以 "dum" 开头的 → 匹配测试中的 dummy 引擎 → 生成 `["!dummy_engine", "!dummy_private_engine"]`
- `autocomplete_list`：非空
- `autocomplete_location`：`(user_query_parts, 2)`（❗ 注意：不是 `query_parts`，因为 `!dum` 的 special_part=False，归属是 `user_query_parts`）
- `get_autocomplete_full_query("!dummy_engine")`：
  - 替换 `user_query_parts[2]`（即 `!dum`）为 `"!dummy_engine"`
  - 此时 `query_parts = []`（没有任何片段的 special_part 为 True）
  - `user_query_parts = ['the', 'query', '!dummy_engine']`
  - `getFullQuery()` = `' '.join([]) + ' ' + 'the query !dummy_engine'` = **`"the query !dummy_engine"`**
  - ⚠️ **没有提升到前面**！因为 `!dum` 的 found=False，它不在 `query_parts` 中，补全文本仍停留在末尾位置

是否调用外部后端：❌ 否（`autocomplete_list` 非空）

参考测试：[test_query.py#L232-L234](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_query.py#L232-L234)（测试只断言了 `autocomplete_list`，未断言 `get_autocomplete_full_query` 的结果）

**对照**：如果用户输入的是 `"the query !dummy_engine"`（完整有效引擎名），则：
- found=True → special_part=True → `!dummy_engine` 归入 `query_parts`
- `autocomplete_location = (query_parts, 0)`
- `getFullQuery()` = `'!dummy_engine' + ' ' + 'the query'` = **`"!dummy_engine the query"`**（提升到前面）

这就是 [test_query.py#L212-L219](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_query.py#L212-L219) 测试的 `test_bang` 用例，其中 `SPECIFIC_BANGS = ['!dummy_engine', '!gd', '!general']` 都是完整有效的引擎名/分类名/快捷方式，found=True。

---

#### 示例 2：`!dum` 在前，`query` 是最后一个 → 补全普通词，调用外部后端

输入：`"!dum the query"`，**逐片段追踪**：

| 片段 | 是否进入解析器 | enable_autocomplete | 解析器 found（=special_part） | 片段归属 | `getQuery()` 包含？ |
|------|--------------|-------------------|---------------------------|---------|-------------------|
| `!dum` | 是（BangParser） | **False**（不是最后一个） | False（dum 不是有效引擎） | `user_query_parts` | ✅ |
| `the` | 否（无前缀） | N/A | False | `user_query_parts` | ✅ |
| `query` | 否（无前缀） | N/A | False | `user_query_parts` | ✅ |
| 汇总 | | | | | `sug_prefix = "!dum the query"`（所有片段都在 user_query_parts） |

补全相关：
- `!dum` 的补全开关关闭（enable_autocomplete=False）→ 不生成任何补全
- 最后一个是普通词 `query`，不进入解析器 → `autocomplete_list = []`
- `autocomplete_location`：`(user_query_parts, 2)`

外部后端调用：✅ 是（`autocomplete_list` 为空，且 `sug_prefix = "!dum the query"` 非空）——注意 sug_prefix 中**包含了未被识别的 `!dum`**，因为它 found=False 留在了 user_query_parts。

参考测试：[test_query.py#L236-L238](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_query.py#L236-L238)

---

#### 示例 3：外部 bang 在末尾 → 补全外部 bang

输入：`"the query !!dd"`，**逐片段追踪**：

| 片段 | 是否进入解析器 | enable_autocomplete | 解析器 found（=special_part） | 片段归属 | `getQuery()` 包含？ |
|------|--------------|-------------------|---------------------------|---------|-------------------|
| `the` | 否 | N/A | False | `user_query_parts` | ✅ |
| `query` | 否 | N/A | False | `user_query_parts` | ✅ |
| `!!dd` | 是（ExternalBangParser，长度>2） | **True** | 取决于 dd 是否为有效 bang：有效→True/无效→False | 有效→`query_parts` / 无效→`user_query_parts` | 有效❌ / 无效✅ |

补全相关：
- enable_autocomplete=True → ExternalBangParser 无条件调用 `_autocomplete(bang_ac_list)`
- `bang_ac_list` 由 Trie 树 BFS 搜索返回（见附录 B）
- 若 bang_ac_list 为空 → 回退到默认 `['g', 'ddg', 'bing']` → 补全为 `['!!g', '!!ddg', '!!bing']`
- `get_autocomplete_full_query("!!ddg")`：替换最后片段为 `!!ddg`，若 `!!ddg` 是有效 bang 则 `!!ddg` 在 query_parts，否则与 `the query` 一同在 user_query_parts，但 `getFullQuery()` 公式保证 query_parts 在前拼接 → `!!ddg the query`

是否调用外部后端：❌ 否（autocomplete_list 非空）

参考测试：[test_query.py#L193-L203](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_query.py#L193-L203)

---

#### 示例 4：`:en` 在前（有效语言），`tuto` 是最后一个 → 补全普通词，调用外部后端

输入：`":en python tuto"`，**逐片段追踪**：

| 片段 | 是否进入解析器 | enable_autocomplete | 解析器 found（=special_part） | 片段归属 | `getQuery()` 包含？ |
|------|--------------|-------------------|---------------------------|---------|-------------------|
| `:en` | 是（LanguageParser） | False（不是最后一个） | **True**（en 是有效语言代码） | ❗ `query_parts`（found=True） | ❌ **排除** |
| `python` | 否（无前缀） | N/A | False | `user_query_parts` | ✅ |
| `tuto` | 否（无前缀） | N/A | False | `user_query_parts` | ✅ |
| 汇总 | | | | | `sug_prefix = "python tuto"`（`:en` 被成功识别为特殊语法，因此排除） |

补全相关：
- `:en` 不是最后一个，enable_autocomplete=False → 不生成补全（即使补全开关打开也不会生成，因为 found=True 时 LanguageParser 不触发 _autocomplete）
- 最后一个是普通词 → `autocomplete_list = []`

外部后端调用：✅ 是（对 `sug_prefix = "python tuto"` 进行搜索补全，且语言标签 `:en` 已经被移除）

---

#### 示例 5（反例）：`:en` 是最后一个且有效 → 意外调用外部后端

输入：`"python :en"`（易出错场景），**逐片段追踪**：

| 片段 | 是否进入解析器 | enable_autocomplete | 解析器 found | special_part | 片段归属 | `getQuery()` 包含？ |
|------|--------------|-------------------|------------|-------------|---------|-------------------|
| `python` | 否 | N/A | False | False | `user_query_parts` | ✅ |
| `:en` | 是（LanguageParser） | **True**（最后一个） | **True**（en 有效） | True | `query_parts` | ❌ 排除 |

补全相关：
- enable_autocomplete=True，但 **`not found = False`**（因为 en 是有效语言）
- LanguageParser 的触发条件是 `enable_autocomplete and not found` → False AND False = **不触发补全**
- 所以 `autocomplete_list = []`

外部后端调用：✅ **调用外部后端**，sug_prefix = `"python"`（`:en` 作为有效语言被排除了）

这与直觉相反——用户输入 `:en` 明明是语言语法，但因为它恰好是合法语言代码，**反而不触发语言建议补全，直接进入搜索建议补全**。

---

#### 示例 6（反例）：`<3` 是最后一个且有效 → 意外调用外部后端

输入：`"python <3"`，**逐片段追踪**：

| 片段 | 是否进入解析器 | enable_autocomplete | value | `not value` | found（数字<100） | special_part | 片段归属 | sug_prefix 包含？ |
|------|--------------|-------------------|-------|------------|----------------|-------------|---------|----------------|
| `python` | 否 | N/A | N/A | N/A | False | False | `user_query_parts` | ✅ |
| `<3` | 是（TimeoutParser） | **True** | `"3"`（非空） | **False** | True（3<100，设置 timeout_limit=3.0） | True | `query_parts` | ❌ 排除 |

补全触发条件：`enable_autocomplete and not value` = True AND **False** = **不触发补全** → `autocomplete_list = []`

外部后端调用：✅ **调用外部后端**，sug_prefix = `"python"`

同样与直觉相反——用户输入 `<3` 明明是超时语法，但 value 非空就不会触发超时建议补全，转而直接进入搜索补全。

### 「位置提升」现象

`getFullQuery()` 的拼接公式为：
```python
return '{0} {1}'.format(' '.join(self.query_parts), self.getQuery()).strip()
#                  ↑ 特殊语法在前                ↑ 普通搜索词在后
```

因此无论用户把特殊语法片段输入在什么位置，当补全建议替换最后一个片段后，**所有 `query_parts` 都会出现在最终查询的最前面**，而 `user_query_parts` 则紧随其后。这就是测试中 `"the query !dum"` 的补全结果是 `"!dummy_engine the query"`（`!dummy_engine` 被提升到前面）的原因。

### ★ 核心决策：内部语法补全 vs 外部搜索建议

`webapp.py` 路由层的判断非常简单（[webapp.py#L831](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/webapp.py#L831)）：

```python
if len(raw_text_query.autocomplete_list) == 0 and len(sug_prefix) > 0:
    # → 调用外部搜索后端（search_autocomplete）
    ...
if len(raw_text_query.autocomplete_list) > 0:
    # → 使用内部语法补全
    ...
```

也就是说，**切换的唯一依据是 `autocomplete_list` 是否为空**，而 `autocomplete_list` 是否为空由以下两个条件的**逻辑与**决定：
1. 最后一个非空片段**匹配了某解析器**的 `check()` 条件
2. 该解析器的**补全触发条件**被满足（每个解析器的条件差异巨大）

**各解析器补全触发条件的精确差异**（必须 `enable_autocomplete=True` 即「是最后一个片段」为前提）：

| 解析器 | check 条件 | 补全触发条件（__call__ 内部） | 代码位置 | found=True 时补全吗？ | found=False 时补全吗？ |
|--------|-----------|------------------------------|---------|----------------------|----------------------|
| **TimeoutParser** | 首字符为 `<` | `if enable_autocomplete **and not value**` | [query.py#L51](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L51) | 不相关，只看 value 是否为空 |
| **LanguageParser** | 首字符为 `:` | `if enable_autocomplete **and not found**` | [query.py#L80](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L80) | ❌ **不补全** | ✅ 补全 |
| **ExternalBangParser** | 以 `!!` 开头且长度>2 | `if enable_autocomplete`（**无条件**） | [query.py#L159](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L159) | ✅ 补全（即使 bang 完全有效） | ✅ 补全 |
| **BangParser** | 首字符为 `!` 且不是 `!!` | `if enable_autocomplete`（**无条件**） | [query.py#L189](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L189) | ✅ 补全（即使引擎名完全有效） | ✅ 补全 |
| **FeelingLuckyParser** | 恰好等于 `!!` | 无补全逻辑 | [query.py#L240-L247](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L240-L247) | —（无补全） | —（无补全） |

⚠️ **关键反例（极易出错的场景）**：

| 输入 | 最后片段 | 解析器 | 补全触发条件结果 | autocomplete_list | 最终行为 |
|------|---------|--------|---------------|-------------------|---------|
| `"hello :en"` | `:en` | LanguageParser | found=True（en 有效）→ **not found = False** | `[]`（空！） | ✅ **调用外部后端**（对 `"hello"` 进行补全，`:en` 被作为特殊语法排除） |
| `"hello :xyz"` | `:xyz` | LanguageParser | found=False（xyz 无效）→ **not found = True** | 非空 | ❌ 用内部语法补全 |
| `"hello <3"` | `<3` | TimeoutParser | value = "3" → **not value = False** | `[]`（空！） | ✅ **调用外部后端**（对 `"hello"` 进行补全，`<3` 被解析为超时） |
| `"hello <"` | `<` | TimeoutParser | value = "" → **not value = True** | 非空 | ❌ 用内部语法补全 |
| `"hello <abc"` | `<abc` | TimeoutParser | value = "abc" → **not value = False**，且 found=False（abc 非数字）→ **special_part=False** | `[]` 且 `<abc` 留在 user_query_parts | ✅ **调用外部后端**（对 `"hello <abc"` 整体补全） |
| `"hello !google"` | `!google` | BangParser | 无条件补全 | 非空 | ❌ 用内部语法补全（即使 `google` 是有效引擎名） |
| `"hello !!"` | `!!` | FeelingLuckyParser | 无补全逻辑 | `[]` | ✅ **调用外部后端**（同时启用 I'm Feeling Lucky 跳转） |

### 补全行为速查表（精确版）

| 最后一个片段 | 具体情况 | `enable_autocomplete` | `autocomplete_list` | 是否调用外部后端 |
|-------------|---------|----------------------|--------------------|----------------|
| **普通搜索词**（无前缀） | 任意 | N/A（不进入解析器） | ❌ 空 | ✅ |
| **`<`** 单独一个字符 | value="" | ✅ | ✅ 非空（`<3`, `<850`） | ❌ |
| **`<有效数字>`**（如 `<3`, `<850`） | value 非空且全数字 | ✅ | ❌ 空（解析成功但不触发补全） | ✅（去掉超时设置后补全剩余词） |
| **`<非数字>`**（如 `<abc`） | value 非空且含非数字字符 | ✅ | ❌ 空（special_part=False，留在普通词里） | ✅（连同 `<abc` 一起作为普通搜索词补全） |
| **`:` 单独一个字符** | value="" → found=False | ✅ | ✅ 非空（示例语言代码） | ❌ |
| **`:有效语言码`**（如 `:en`, `:zh-CN`） | found=True | ✅ | ❌ 空（解析成功不触发补全） | ✅（去掉语言标签后补全剩余词） |
| **`:无效/部分语言`**（如 `:englis`, `:xyz`） | found=False | ✅ | ✅ 非空（匹配的语言建议） | ❌ |
| **`!!任意字符`**（长度≥3，如 `!!g`, `!!dd`） | 无条件补全 | ✅ | ✅ 非空（空时给默认 `g/ddg/bing`） | ❌ |
| **`!!` 恰好两个** | 进入 FeelingLuckyParser，无补全 | ✅ | ❌ 空 | ✅（同时启用首结果跳转） |
| **`!` 单独一个字符** | 无条件补全 | ✅ | ✅ 非空（默认 `images/wikipedia/osm`） | ❌ |
| **`!任意字符`**（如 `!g`, `!goo`, `!google`） | 无条件补全 | ✅ | ⚠️ **取决于匹配结果**：有匹配→非空→❌不调用外部；**无匹配→空→✅调用外部** | ⚠️ 不确定（见下方说明） |

⚠️ **BangParser 与 ExternalBangParser 的 fallback 差异**（速查表的关键补充）：

- **ExternalBangParser**（`!!` 前缀）：`_autocomplete()` 中有 fallback 逻辑——当 Trie 搜索结果为空时，回退到默认 `['g', 'ddg', 'bing']`（[query.py#L147-L149](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L147-L149)）。因此 `!!任意字符` 的 autocomplete_list **始终非空**。
- **BangParser**（`!` 前缀，非 `!!`）：`_autocomplete()` **没有 fallback 逻辑**。当 value 非空但没有任何引擎/分类/快捷方式以该 value 开头时，不会添加任何建议，autocomplete_list **为空**。因此 `!zzzzz`（假设没有引擎名以 zzzzz 开头）会进入外部搜索。

---

## 附录 B：外部 bang 建议的遍历顺序、去重与排序

外部 bang（`!!` 前缀）的建议生成逻辑最为复杂，涉及 **分段 Trie 前缀树**、**BFS 层序遍历**、**三级去重**和**最终排序覆盖**。核心实现位于 [external_bang.py#L64-L90](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L64-L90)。

### 核心概念区分（极易混淆）

在深入分析前，必须先明确三个完全独立的概念：

| 概念 | 生成方式 | 作用 | 示例（TEST_DB 中） |
|------|---------|------|------------------|
| **bang 名** | 由 `get_node()` 的 `before + 子分段键` 逐段拼接得到 | 用户在搜索框中输入的 bang 字符串（即 `!!xxx` 中的 xxx 部分） | `seascapes`（路径 `sea→s→capes` 拼接：`sea`+`s`+`capes`） |
| **节点子键** | JSON/Trie 数据中硬编码的分段键 | Trie 树的内部索引结构 | `capes`（不是完整 bang 名） |
| **叶子值** | JSON/Trie 数据中 str 节点的内容 | 存储 URL 模板和 rank，格式为 `URL模板chr(2)chr(1)rank` | `'seascape' + chr(2) + chr(1) + '0'`（其中 URL 前缀为 `seascape`，注意是**单数**，与 bang 名 `seascapes` 不同） |

⚠️ **最关键的区分**：`seascapes`（bang 名，复数） ≠ `seascape`（叶子值中的 URL 前缀，单数）。两者只是碰巧相似，没有必然的对应关系。

### 数据结构基础（精确对应 TEST_DB）

外部 bang 数据库以**分段 Trie** 结构存储。两个特殊控制字符：

- `LEAF_KEY = chr(16)`：在 dict 节点中标记「该前缀本身就是完整 bang」
- `chr(2)`（STX）：叶子值中 URL 模板与查询占位符的分隔符（替换为 URL 编码后的搜索词）
- `chr(1)`（SOH）：叶子值中 URL 部分与 rank 部分的分隔符

叶子值编码规则：`URL模板chr(2)chr(1)rank值`

例如 `'seascape' + chr(2) + chr(1) + '0'` 拆解为：
- `URL模板 = 'seascape'`
- `chr(2)`：查询占位符（填入搜索词）
- `chr(1)`：URL/rank 分隔符
- `rank = '0'`

**TEST_DB 精确结构**（[test_external_bangs.py#L15-L34](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_external_bangs.py#L15-L34)）：

```
TEST_DB = {'trie': {
  ├─ 'exam' → {                                      # 分段键 'exam'
  │     ├─ chr(16) → '//wikipedia.org/wiki/' + chr(2) + chr(1) + '0'
  │     │                ↑ URL模板               ↑ chr(2)占位符   ↑ chr(1)分隔  ↑ rank=0
  │     │                表示 bang 名 'exam' 的完整定义
  │     └─ 'ple' → '//example.com/' + chr(2) + chr(1) + '0'
  │                  bang 名 = 'exam'+'ple' = 'example'，叶子值含 URL 和 rank=0
  └─ 'sea' → {                                       # 分段键 'sea'
        ├─ chr(16) → 'sea' + chr(2) + chr(1) + '0'  # bang 名 'sea' 本身有完整定义
        ├─ 'rch' → {                                 # 分段键 'rch'，对应 'sea'+'rch'='search'
        │     ├─ chr(16) → 'search' + chr(2) + chr(1) + '0'   # bang 名 'search'
        │     └─ 'ing' → 'searching' + chr(2) + chr(1) + '0'  # bang 名 'search'+'ing'='searching'
        └─ 's' → {                                   # 分段键 's'，对应 'sea'+'s'='seas'（中间节点，非完整 bang）
              ├─ 'on' → 'season' + chr(2) + chr(1) + '0'      # bang 名 'seas'+'on'='season'
              └─ 'capes' → 'seascape' + chr(2) + chr(1) + '0' # bang 名 'seas'+'capes'='seascapes'
  }
}}
```

### ★ resolve_bang_definition() 解析规则

位置：[external_bang.py#L49-L61](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L49-L61)

```python
def resolve_bang_definition(bang_definition: str, query: str) -> tuple[str, int]:
    url, rank = bang_definition.split(chr(1))      # ① chr(1) 拆分 URL 和 rank
    if url.startswith('//'):
        url = 'https:' + url                       # ② 补协议头
    if query:
        url = url.replace(chr(2), quote_plus(query)) # ③ chr(2) 替换为 URL 编码的搜索词
    else:
        o = urlparse(url)                          # ④ 无查询词时截断到域名根路径
        url = o.scheme + '://' + o.netloc
    rank = int(rank) if len(rank) > 0 else 0      # ⑤ rank 转整数
    return (url, rank)
```

⚠️ **关键事实**：**BFS 中调用 resolve_bang_definition 时 query 参数传的是空串 `''`**（见 external_bang.py L81：`resolve_bang_definition(current_bang_definition, '')`），因此永远走分支④，只解析 rank，URL 的具体值不影响补全结果。

### ★ get_node() 的精确行为（理解遍历的关键）

位置：[external_bang.py#L17-L27](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L17-L27)

```python
def get_node(external_bangs_db, bang):
    node = external_bangs_db['trie']
    after = ''
    before = ''
    for bang_letter in bang:     # ★ 逐字符遍历输入 bang
        after += bang_letter
        if after in node and isinstance(node, dict):
            node = node[after]   # ★ 命中分段键，进入子节点
            before += after      # 累积已完全匹配的前缀 = 目前拼出的 bang 名前缀
            after = ''           # 重置「尚未命中的尾部」
    return node, before, after   # ★ 返回 (当前节点指针, 已拼出的前缀, 未命中的尾部字符)
```

TEST_DB 输入示例追踪：

| 输入 bang | 逐字符扫描过程 | node 最终指向 | before | after |
|-----------|---------------|--------------|--------|-------|
| `sea` | s→e→a → after='sea' 命中 root 子键 'sea' → before='sea'，after 重置为 '' | `TEST_DB['trie']['sea']`（dict，含 3 个子键） | `'sea'` | `''` |
| `sear` | s→e→a 命中 'sea'，before='sea', after=''；继续 r → after='r' 不命中 sea 的子键（sea 子键是 chr(16), 'rch', 's'，没有 'r'） | `TEST_DB['trie']['sea']`（dict） | `'sea'` | `'r'` |
| `search` | s→e→a 命中 'sea'；r→c→h → after='rch' 命中 sea 子键 'rch' → before='search', after='' | `TEST_DB['trie']['sea']['rch']`（dict，含 chr(16) 和 'ing'） | `'search'` | `''` |
| `seas` | s→e→a 命中 'sea'；s → after='s' 命中 sea 子键 's' → before='seas', after='' | `TEST_DB['trie']['sea']['s']`（dict，含 'on' 和 'capes'） | `'seas'` | `''` |
| `seascapes` | s→e→a 命中；s 命中；c→a→p→e→s → after='capes' 命中 seas 子键 'capes' → node 是 str 叶子（值 `'seascape'+chr(2)+chr(1)+'0'`） | `str叶子`（**不是 dict**） | `'seascapes'` | `''` |
| `sezz` | s→e→a 命中；继续 z→z → after='zz' 不命中 sea 子键 | `TEST_DB['trie']['sea']`（dict） | `'sea'` | `'zz'` |

### get_bang_definition_and_ac() — 一层候选生成

位置：[external_bang.py#L30-L46](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L30-L46)

根据 `get_node()` 返回的 node/before/after 组合生成**一层深度**的候选列表：

```python
if after != '':
    # 状态①：部分匹配（尾部有未命中字符）。只返回同层中以 after 开头的兄弟分段键，与 before 拼接
    for k in node:
        if k.startswith(after):
            bang_ac_list.append(before + k)
elif isinstance(node, dict):
    # 状态②：精确命中 dict 节点。若该节点有 LEAF_KEY，说明 before 本身是完整 bang；
    #        返回所有子分段键（排除 LEAF_KEY）与 before 的拼接，作为候选下一步
    bang_definition = node.get(LEAF_KEY)
    bang_ac_list = [before + k for k in node.keys() if k != LEAF_KEY]
elif isinstance(node, str):
    # 状态③：精确命中叶子（str）。before 本身就是完整 bang，无子节点可走
    bang_definition = node
    bang_ac_list = []
```

⚠️ **该函数只返回一层深度的候选**，不会递归。但通过 `bang_definition` 返回值记录「输入 bang 本身是否为完整可跳转 bang」。

### ★ BFS 遍历顺序（get_bang_definition_and_autocomplete）

位置：[external_bang.py#L64-L90](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L64-L90)

```python
bang_definition, bang_ac_list = get_bang_definition_and_ac(external_bangs_db, bang)

new_autocomplete = []
current = [*bang_ac_list]            # ★ 初始队列：第一层候选（一层展开的结果）
done = set()
while len(current) > 0:
    bang_ac = current.pop(0)         # ★ 队首出队 → 标准 FIFO（层序遍历）
    done.add(bang_ac)                # 出队即标记为已处理（第 1 层去重）

    current_bang_definition, current_bang_ac_list = get_bang_definition_and_ac(external_bangs_db, bang_ac)
    if current_bang_definition:      # 该候选 bang 本身有完整定义？收集其 rank
        _, order = resolve_bang_definition(current_bang_definition, '')
        new_autocomplete.append((bang_ac, order))   # ★ 存的是 bang 名（before 拼接），不是叶子值
    for new_bang in current_bang_ac_list:
        if new_bang not in done and new_bang not in current:  # ★ 入队前双重检查（第 2 层去重）
            current.append(new_bang)   # ★ 追加到队尾

new_autocomplete.sort(key=lambda t: (-t[1], t[0]))   # ★ 最终排序完全覆盖遍历顺序
```

#### 遍历顺序的三个核心事实

| 结论 | 代码依据 | 影响 |
|------|---------|------|
| **① FIFO 层序遍历** | `pop(0)`（队首出） + `append`（队尾入） | 第一层先处理，再处理第一层各节点展开的子节点……严格按 Trie 树深度展开 |
| **② 初始顺序由 dict.keys() 决定** | `[*bang_ac_list]` 直接拷贝列表 | 第一层候选顺序 = Trie JSON 数据的存储顺序（Python 3.7+ dict 保证插入序）；该顺序只影响「多个候选路径汇聚时哪个先被 `done.add()` 锁定」，**不影响最终排序结果** |
| **③ BFS 顺序不影响最终输出** | 末尾有 `sort(key=(-rank, 名称))` | 即使 BFS 完全乱序处理，最终 sort() 会按 `(-rank, 字典序)` 重新排列，**BFS 遍历顺序对最终输出结果零影响**，唯一影响是去重时的先到先锁定 |

### 去重机制（三层架构）

| 层级 | 实现代码 | 作用域 | 作用 |
|------|---------|--------|------|
| **第 1 层** | `done.add(bang_ac)`（出队立即标记） | BFS 遍历期间（external_bang.py 内） | 防止同一个 bang 名被重复处理 |
| **第 2 层** | `if new_bang not in done and new_bang not in current`（入队前检查） | BFS 队列维护期间 | 防止同一节点被多次加入等待队列，避免队列冗余膨胀 |
| **第 3 层** | `_add_autocomplete()` 中 `if value not in autocomplete_list` | ExternalBangParser._autocomplete（query.py 层） | 跨解析器去重，防止其他解析器写入了相同的建议文本 |

代码对照：
- 第 1、2 层：[external_bang.py#L72-L85](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L72-L85)
- 第 3 层：[query.py#L171-L175](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L171-L175) 结合 [query.py#L38-L40](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L38-L40)

### 排序规则（最终输出的唯一决定因素）

排序位于 [external_bang.py#L87-L88](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/external_bang.py#L87-L88)：

```python
new_autocomplete.sort(key=lambda t: (-t[1], t[0]))
new_autocomplete = list(map(lambda t: t[0], new_autocomplete))
```

| 关键字 | 方向 | 含义 |
|--------|------|------|
| 第一关键字 `-rank` | **降序**（取负实现） | rank 越大越流行，越靠前；rank 来自 `bang_definition.split(chr(1))[1]`，即叶子值中 `chr(1)` 之后的部分 |
| 第二关键字 `bang名称` | **字典序升序** | rank 相同时的 tie-breaker，按 bang 名（before 路径拼接结果）逐字符比较，保证稳定可重复 |

### ★ 完整示例追踪（逐行对照代码，与测试断言严格一致）

输入：`sea`，使用 `TEST_DB`

---

**阶段 0 — 预处理**：调用 `get_bang_definition_and_ac('sea', TEST_DB)`

| 步骤 | 结果 |
|------|------|
| `get_node(TEST_DB, 'sea')` | before='sea', after='', node=TEST_DB['trie']['sea']（dict） |
| node 类型判断 | after='' 且 isinstance(node, dict) → 状态② |
| `bang_definition` | `node.get(chr(16)) = 'sea' + chr(2) + chr(1) + '0'`（非空）→ 'sea' 本身是完整 bang ✅ |
| `bang_ac_list` | `node.keys()` = `[chr(16), 'rch', 's']`，排除 LEAF_KEY → `['rch', 's']`，分别与 before='sea' 拼接 → `['sear', 'ses']` |

⚠️ **关键事实**：'sea' 本身虽然是完整 bang，但它是**输入参数**，返回值通过 `bang_definition` 单独记录，**永远不会被加入 BFS 队列和补全列表**！只有 `sear` 和 `ses` 是候选起点。

---

**阶段 1 — BFS 初始状态**：
- `current = ['sear', 'ses']`（由 `bang_ac_list` 浅拷贝）
- `done = ∅`
- `new_autocomplete = []`（空，sea 本身不进入此列表）

---

**阶段 2 — BFS 循环**：

🔹 **轮次 1：出队 `sear`**
- `done.add('sear')` → `done = {'sear'}`
- `get_bang_definition_and_ac(TEST_DB, 'sear')`:
  - `get_node()`：s→e→a 命中 'sea'；r 不命中 sea 子键 → `before='sea', after='r', node=sea dict`
  - after='r' != '' → 状态①（部分匹配）
  - 遍历 sea 子键 `[chr(16), 'rch', 's']`：
    - `chr(16).startswith('r')` → ❌
    - `'rch'.startswith('r')` → ✅ → 拼接 `before + 'rch' = 'sear' + 'ch'? 不，是 'sea' + 'rch' = 'search'`
    - `'s'.startswith('r')` → ❌
  - → `current_bang_definition = None`（**非完整 bang**，没有对应 LEAF 定义）
  - → `current_bang_ac_list = ['search']`
- `current_bang_definition` 为空 → **不**加入 `new_autocomplete`
- 子节点 `['search']`：'search' ∉ done 且 ∉ current → ✅ 入队
- **此时状态**：`current = ['ses', 'search']`，`new_autocomplete = []`

---

🔹 **轮次 2：出队 `ses`**
- `done.add('ses')` → `done = {'sear', 'ses'}`
- `get_bang_definition_and_ac(TEST_DB, 'ses')`:
  - `get_node()`：s→e→a 命中 'sea'；s 命中 sea 子键 's' → before='seas', after='', node=sea.s dict
  - after='' 且 node 是 dict → 状态②
  - sea.s dict keys = `['on', 'capes']`，无 LEAF_KEY → `current_bang_definition = None`（**中间节点，非完整 bang**）
  - 子键排除 LEAF_KEY（不存在）后拼接 → `current_bang_ac_list = ['seas'+'on', 'seas'+'capes'] = ['season', 'seascapes']`
- `current_bang_definition` 为空 → **不**加入 `new_autocomplete`
- 子节点 `['season', 'seascapes']`：两者均不在 done / current → ✅ 全部入队
- **此时状态**：`current = ['search', 'season', 'seascapes']`，`new_autocomplete = []`

---

🔹 **轮次 3：出队 `search`**
- `done.add('search')` → `done = {'sear', 'ses', 'search'}`
- `get_bang_definition_and_ac(TEST_DB, 'search')`:
  - `get_node()`：s→e→a 命中 'sea'；r→c→h 命中 sea 子键 'rch' → before='search', after='', node=sea.rch dict
  - after='' 且 node 是 dict → 状态②
  - sea.rch dict keys = `[chr(16), 'ing']`
  - `current_bang_definition = node[chr(16)] = 'search' + chr(2) + chr(1) + '0'`（**完整 bang** ✅）
  - 排除 LEAF_KEY 后子键为 'ing' → `current_bang_ac_list = ['search'+'ing'] = ['searching']`
- `current_bang_definition` 非空 → 调用 `resolve_bang_definition('search' + chr(2) + chr(1) + '0', '')`:
  - split(chr(1)) → `['search' + chr(2), '0']` → rank = int('0') = 0
  - → `new_autocomplete.append(('search', 0))`（**bang 名是 'search'，与叶子值前缀相同只是巧合**）
- 子节点 `['searching']`：不在 done/current → ✅ 入队
- **此时状态**：`current = ['season', 'seascapes', 'searching']`，`new_autocomplete = [('search', 0)]`

---

🔹 **轮次 4：出队 `season`**
- `done.add('season')`
- `get_bang_definition_and_ac(TEST_DB, 'season')`:
  - `get_node()`：s→e→a 命中 'sea'；s 命中 's'；o→n 命中 seas 子键 'on' → node 是 `str叶子 = 'season' + chr(2) + chr(1) + '0'`（**精确命中叶子**）
  - after='' 且 isinstance(node, str) → 状态③
  - `current_bang_definition = node = 'season' + chr(2) + chr(1) + '0'`（完整 bang ✅）
  - `current_bang_ac_list = []`（叶子无子节点）
- 非空 → `resolve_bang_definition(...)` → rank=0 → `new_autocomplete.append(('season', 0))`
- 无子节点 → 无入队
- **此时状态**：`current = ['seascapes', 'searching']`，`new_autocomplete = [('search', 0), ('season', 0)]`

---

🔹 **轮次 5：出队 `seascapes`**
- `done.add('seascapes')`
- `get_bang_definition_and_ac(TEST_DB, 'seascapes')`:
  - `get_node()`：s→e→a 命中；s 命中；c→a→p→e→s 命中 seas 子键 'capes' → node 是 `str叶子 = 'seascape' + chr(2) + chr(1) + '0'`
  - after='' 且 isinstance(node, str) → 状态③
  - `current_bang_definition = 'seascape' + chr(2) + chr(1) + '0'`（**完整 bang ✅**）
  - `current_bang_ac_list = []`
- 非空 → `resolve_bang_definition(...)` → rank=0 → `new_autocomplete.append(('seascapes', 0))`
- ⚠️ **关键区分**：这里加入 new_autocomplete 的是 bang 名 `'seascapes'`（before 路径拼接：sea+s+capes），**不是**叶子值中的 URL 前缀 `'seascape'`（单数）。两者只是碰巧相似，没有必然联系。
- **此时状态**：`current = ['searching']`，`new_autocomplete = [('search', 0), ('season', 0), ('seascapes', 0)]`

---

🔹 **轮次 6：出队 `searching`**
- `done.add('searching')`
- `get_bang_definition_and_ac(TEST_DB, 'searching')`:
  - `get_node()`：s→e→a 命中；r→c→h 命中 'rch'；i→n→g 命中 search 子键 'ing' → node 是 `str叶子 = 'searching' + chr(2) + chr(1) + '0'`
  - state③ → `current_bang_definition = 叶子值`（完整 bang ✅），`current_bang_ac_list = []`
- 非空 → `resolve_bang_definition(...)` → rank=0 → `new_autocomplete.append(('searching', 0))`
- **此时状态**：`current = []`（空）→ 循环结束

---

**阶段 3 — 最终排序**：
- BFS 结束时的收集顺序（受 FIFO 影响）：
  ```python
  new_autocomplete = [('search', 0), ('season', 0), ('seascapes', 0), ('searching', 0)]
  ```
- 执行 `sort(key=lambda t: (-t[1], t[0]))`：
  - 所有 rank = 0，第一关键字 `-rank` 全相等
  - 按第二关键字（bang 名）字典序升序：
    - `'search' < 'searching'`（比较到第 7 位：`'' < 'i'`）✅
    - `'searching' < 'seascapes'`（第 4 位：`'a' < 'c'`，因为 searching 是 s-e-a-r-c-h-i-n-g，而 seascapes 是 s-e-a-s-c-a-p-e-s，比较到第 4 位：r < s → searching < seascapes？等一下，逐字符比较：
      - `'searching'` = s e a **r** c h i n g
      - `'seascapes'` = s e a **s** c a p e s
      - 前 3 位 s e a 相同
      - 第 4 位：**r（ASCII 114） < s（ASCII 115）** → 所以 `'searching' < 'seascapes'` ✅
    - `'seascapes' < 'season'`（前 4 位 s e a s 相同，第 5 位：c < o）✅
  - 排序后：
    ```python
    [('search', 0), ('searching', 0), ('seascapes', 0), ('season', 0)]
    ```
- 提取 bang 名列表：**`['search', 'searching', 'seascapes', 'season']`** ✅

---

对照测试断言 [test_external_bangs.py#L98-L101](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/tests/unit/test_external_bangs.py#L98-L101)：
```python
self.assertEqual(
    bang_definition_and_autocomplete,
    (TEST_DB['trie']['sea'][LEAF_KEY], ['search', 'searching', 'seascapes', 'season'])
)
```
完全一致 ✅（包括 `bang_definition` = sea 的 LEAF 值，以及最终 autocomplete 列表）

### 空输入回退逻辑

在 [query.py#L171-L175](file:///d:/fz/0601-1/solo-dogfeeding/code/39-searxng/searx/query.py#L171-L175)：

```python
def _autocomplete(self, bang_ac_list):
    if not bang_ac_list:
        # 当 Trie 中完全无匹配（如 `!!zzzzz`）时，回退到3个热门 bang
        bang_ac_list = ['g', 'ddg', 'bing']
    for external_bang in bang_ac_list:
        # 经过第 3 层去重（_add_autocomplete 检查 autocomplete_list 内不重复）
        self._add_autocomplete('!!' + external_bang)
```
