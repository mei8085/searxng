# SearXNG 输出格式切换机制分析

## 一、机制总览

SearXNG 支持四种搜索结果输出格式：HTML 页面、JSON 数据、CSV 表格和 RSS 订阅。同一份搜索结果可以在这四种格式之间切换，其核心是一套"参数来源与决策-入口状态传递-序列化信息映射"的三层协作机制。

参数来源与决策层决定格式参数从哪里来、各来源优先级如何、三道关口怎样裁决；入口状态传递层决定下载按钮和 RSS 链接实际保留了哪些搜索上下文；序列化信息映射层决定四种格式各自真正保留和丢弃了哪些信息，以及在结果字段之外还携带了哪些附加元数据。

---

## 二、格式参数的来源与决策优先级

### 2.1 参数来源与合并顺序

搜索请求的参数来自多个层次，按照"低优先级先写入，高优先级后覆盖"的顺序逐层合并。但不同类型的参数有不同的合并路径，不能一概而论。

#### 普通搜索参数的合并

查询词、格式名、页码这类不属于用户偏好的参数，走的是简单的表单合并路径：

1.  **HTTP 表单参数（POST Body）**：最先写入，作为基础值
2.  **URL 查询参数（GET Query String）**：随后补充，仅在 POST 中没有同名字段时才生效

也就是说，如果同一个参数同时出现在 POST 表单和 URL 查询串中，**POST 的值优先**，URL 的值不会覆盖表单值。URL 只起到"填空"的作用。

> **代码出处**：[webapp.py#L484-L487](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L484-L487)（POST 与 GET 的合并）

#### 用户偏好参数的合并

安全搜索级别、界面语言、分类过滤这类保存在用户偏好中的参数，走的是更复杂的四层合并路径：

1.  **系统默认值**：实例配置文件中定义的默认值，优先级最低
2.  **Cookie 持久化偏好**：用户之前保存在 Cookie 中的设置，优先级高于默认值
3.  **当前请求的表单参数**：本次请求中携带的偏好字段，会覆盖 Cookie 中的值
4.  **编码后的偏好字符串**：如果请求携带了特殊的偏好编码字段，会解码后整体覆盖，优先级最高
5.  **浏览器语言头回退**：仅当以上来源都未提供语言和区域设置时，才从 `Accept-Language` 请求头推断，作为最终兜底

也就是说，**当前请求的表单参数会覆盖 Cookie 中的持久化偏好**，而不是相反。用户每次提交搜索时携带的偏好参数，会临时覆盖其保存的设置，但不会写回 Cookie——只有在偏好设置页显式保存时才会更新 Cookie。

> **代码出处**：[webapp.py#L476-L476](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L476-L476)（Cookie 解析）、[webapp.py#L489-L496](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L489-L496)（表单覆盖 Cookie）、[webapp.py#L500-L502](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L500-L502)（Accept-Language 回退）

#### 格式参数的实际竞争

`format` 参数属于普通搜索参数，不属于用户偏好，因此它不会出现在 Cookie 中，也不会走偏好合并路径。它的实际竞争只在 POST 表单和 GET 查询串两层之间展开，且 POST 优先级高于 GET。

### 2.2 格式决策的三道关口

从合并后的请求参数中取出 `format` 值后，会依次经过三道决策关口，每道关口的语义截然不同：

| 关口 | 触发条件 | 处理方式 | 设计意图 |
|------|---------|---------|---------|
| 参数提取与默认值 | 未携带 format 参数 | 默认为 HTML | 面向无声明场景的合理默认 |
| 非法格式回落 | 格式名不在四种硬编码格式中 | 静默重置为 HTML | 面向未知输入的容错降级，"写错了"得到宽容 |
| 配置禁用拦截 | 格式名合法但不在管理员配置的白名单中 | 返回 403 中断请求 | 面向运维策略的访问控制，"越权了"必须拒绝 |

**关键的顺序问题**：非法格式回落发生在配置禁用拦截之前。这意味着如果管理员只开放了 HTML，用户请求一个不存在的格式（如 `yaml`）会先被修正为 `html`，然后顺利通过拦截——用户看到正常的搜索结果页，根本不知道自己的参数被改写过。只有当用户请求一个合法但被禁用的格式（如 `json`）时，才会触发 403。

换言之：**写错了格式名得到宽容，写对了但没权限被严格拦截**。这是一个刻意的设计选择，将"手误"和"越权"区别对待。

> **代码出处**：[webapp.py#L629-L634](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L629-L634)（三道关口）、[settings_defaults.py#L22](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/settings_defaults.py#L22-L22)（硬编码格式列表）

---

## 三、格式入口的搜索状态保留

SearXNG 有两种格式入口：侧栏下载入口和 RSS 自动发现链接。它们各自保留的搜索上下文并不完全相同，甚至分类参数的表达方式都不一样。

### 3.1 侧栏下载入口：保留的状态

HTML 结果页侧栏的每个下载按钮都是一个隐藏表单，通过表单字段将当前搜索的上下文传递给新的格式请求。实际保留的状态包括：

| 状态项 | 传递方式 | 说明 |
|--------|---------|------|
| 查询词 | 单字段 `q` | 完整保留用户输入的搜索关键词 |
| 分类过滤 | 多字段 `category_xxx=1` | 每个选中的分类各占一个字段，值恒为 1 |
| 页码 | 单字段 `pageno` | 保留当前页码，分页状态在格式切换后延续 |
| 界面语言 | 单字段 `language` | 保留当前语言设置 |
| 时间范围 | 单字段 `time_range` | 保留时间过滤条件 |
| 安全搜索级别 | 单字段 `safesearch` | 保留安全搜索过滤强度 |
| 超时限制 | 单字段 `timeout_limit` | 可选字段，有自定义值时才传递 |
| 目标格式 | 单字段 `format` | 指定要切换到的目标格式 |

**不保留的状态**：

- 单个引擎的启用/禁用状态（只保留分类级别的过滤）
- 插件的启用/禁用状态
- 主题、布局等纯 UI 偏好
- 结果排序方式（始终使用默认排序）

### 3.2 RSS 自动发现链接：保留的状态

HTML 页面头部的 RSS 订阅链接是一个构造好的 URL 查询字符串，它实际保留的状态包括：

| 状态项 | 传递方式 | 说明 |
|--------|---------|------|
| 查询词 | 单字段 `q` | 完整保留并做 URL 编码 |
| 分类过滤 | 单字段 `categories=a,b,c` | 所有分类逗号拼接成一个参数 |
| 页码 | 单字段 `pageno` | 保留当前页码 |
| 时间范围 | 单字段 `time_range` | 保留 |
| 界面语言 | 单字段 `language` | 保留 |
| 安全搜索级别 | 单字段 `safesearch` | 保留 |
| 目标格式 | 单字段 `format` | 固定为 `rss` |

**不保留的状态**：

- 超时限制（与下载入口不同，RSS 链接从不传递超时参数）
- 单个引擎状态、插件状态和 UI 偏好（与下载入口一致）

### 3.3 两者的差异对比

两种入口最核心的差异有两处：

一是**分类参数的表达方式完全不同**。侧栏下载入口用多字段开关形式（`category_general=1&category_images=1`），RSS 链接用单字段逗号分隔形式（`categories=general,images`）。这两种形式在后端解析时走不同的代码路径，但最终效果等价。

二是**与格式白名单的联动性不同**。侧栏下载入口由白名单动态生成，白名单之外的格式根本不会出现按钮；RSS 自动发现链接是硬编码在模板中的，不受白名单控制，即使管理员禁用了 RSS，这个链接仍然存在——只是访问时会被 403 拦截。

| 维度 | 侧栏下载入口 | RSS 自动发现链接 |
|------|-------------|-----------------|
| 传递方式 | POST 表单隐藏字段 | GET URL 查询字符串 |
| 分类表达 | 多字段开关形式 | 单字段逗号拼接 |
| 超时限制 | 可选传递 | 从不传递 |
| 白名单联动 | 动态生成，白名单之外不出现 | 硬编码，不受白名单控制 |
| 与 Cookie 偏好协作 | 表单值会覆盖 Cookie 值 | 查询串值优先级低于 POST，但格式切换通常走 GET |

**关于 Cookie 覆盖的重要提醒**：由于偏好类参数的合并规则是"表单覆盖 Cookie"，入口传递的偏好值（安全搜索、语言等）会临时覆盖用户 Cookie 中的持久化设置。但这种覆盖只对当前请求生效，不会修改用户的 Cookie。

> **代码出处**：[apis.html](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/templates/simple/elements/apis.html)（侧栏下载入口模板）、[results.html#L11](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/templates/simple/results.html#L11-L11)（RSS 自动发现链接）

---

## 四、四种格式的结果信息映射

搜索结果容器中包含丰富的信息：主结果、直接答案、搜索建议、拼写纠错、信息框、无响应引擎、性能统计、每个结果的几十项元数据字段。四种格式各自从这个容器中取走不同的子集，丢掉的信息也各不相同。

### 4.1 顶层结果类别的映射

| 结果类别 | 说明 | JSON | CSV | RSS | HTML |
|---------|------|:----:|:---:|:---:|:----:|
| 主结果列表 | 搜索引擎返回的核心结果 | ✓ | ✓ | ✓ | ✓ |
| 直接答案 | 计算器、翻译、天气等即时回答 | ✓ | ✓ | ✗ | ✓ |
| 搜索建议 | "您是不是要搜"的建议词 | ✓ | ✓ | ✗ | ✓ |
| 拼写纠错 | 拼写修正建议 | ✓ | ✓ | ✗ | ✓ |
| 信息框 | 维基百科摘要等信息框 | ✓ | ✗ | ✗ | ✓ |
| 无响应引擎 | 超时或出错的引擎列表 | ✓ | ✗ | ✗ | ✓ |
| 结果总数估计 | 命中结果总数 | ✓ | ✗ | ✓* | ✓ |
| 查询词原文 | 用户输入的搜索词 | ✓ | ✗ | ✓* | ✓ |

*注：RSS 的总数和查询词放在 channel 元数据中，不是正文条目。

**解读**：
- JSON 是信息最全的格式，顶层八大类别全部保留
- CSV 只保留了"可以按行摊平"的四类（主结果、答案、建议、纠错），其余一概丢弃，甚至连查询词和结果总数都没有
- RSS 只在正文中保留主结果，查询词和总数放在 channel 头部元数据里
- HTML 全部保留，且建议和纠错会被额外处理为带跳转链接的结构化对象

### 4.2 单个结果的字段映射

每个主结果对象包含三十余项字段，四种格式对单个结果字段的取舍差异更大。下表列出主要字段的保留情况：

| 字段 | 说明 | JSON | CSV | RSS | HTML |
|------|------|:----:|:---:|:---:|:----:|
| title | 结果标题 | ✓ | ✓ | ✓ | ✓ |
| url | 结果链接 | ✓ | ✓ | ✓ | ✓ |
| content | 结果摘要 | ✓ | ✓ | ✓ | ✓* |
| engine | 来源引擎名 | ✓ | ✓ | ✗ | ✓ |
| engines | 合并后所有来源引擎 | ✓ | ✗ | ✗ | ✓ |
| score | 结果排序分数 | ✓ | ✓ | ✗ | ✗ |
| host / parsed_url | 主机名 / 结构化 URL | ✓ | ✓(仅主机) | ✗ | ✓ |
| publishedDate | 发布日期（日期对象） | ✓ | ✗ | ✗ | ✓ |
| pubdate | 发布日期字符串 | ✓ | ✗ | ✓ | ✓ |
| category | 结果所属分类 | ✓ | ✗ | ✗ | ✓ |
| template | 使用的渲染模板名 | ✓ | ✗ | ✗ | 内部使用 |
| thumbnail | 缩略图 URL | ✓ | ✗ | ✗ | ✓ |
| img_src | 图片源 URL | ✓ | ✗ | ✗ | ✓ |
| author | 作者 | ✓ | ✗ | ✗ | ✓ |
| metadata | 杂项元数据 | ✓ | ✗ | ✗ | ✓ |
| views | 观看次数 | ✓ | ✗ | ✗ | ✓ |
| length | 时长（音视频） | ✓ | ✗ | ✗ | ✓ |
| iframe_src | 嵌入视频 URL | ✓ | ✗ | ✗ | ✓ |
| audio_src | 音频源 URL | ✓ | ✗ | ✗ | ✓ |
| priority | 结果优先级标记 | ✓ | ✗ | ✗ | ✗ |
| positions | 各引擎中的排名 | ✓ | ✗ | ✗ | ✗ |
| open_group | 分组开启标记 | ✓ | ✗ | ✗ | 内部使用 |
| close_group | 分组关闭标记 | ✓ | ✗ | ✗ | 内部使用 |

*注：HTML 的 content 会被截断到 1024 字符并做关键词高亮标记，不是原始文本。

**解读**：

- **JSON**：完整保留全部 23 项字段，是唯一能还原原始结果对象的格式，也是唯一暴露排序分数、排名位置、优先级等内部元数据的格式
- **CSV**：固定 7 列，取了最通用的几项。其中 host 列是从 URL 中额外提取的主机名，而非原始字段。单个结果中丰富的媒体字段、作者、元数据等全部丢弃
- **RSS**：只有 title、url、content、pubdate 四项正文字段。来源引擎、分数、分类、媒体资源等全部丢失。RSS 是信息损失最大的格式
- **HTML**：实际展示的字段介于 JSON 和 RSS 之间，所有面向用户的字段都会显示（作者、缩略图、嵌入媒体等），但排序分数、排名位置等内部调试信息不暴露给用户

### 4.3 HTML 格式特有的字段转换

HTML 除了字段取舍外，还对两个字段做了不可还原的格式转换：

1.  **关键词高亮与截断**：title 和 content 字段会对搜索关键词做 HTML 标记插入，在关键词周围包裹高亮标签。同时 content 会被截断到 1024 字符。这个转换只发生在 HTML 路径，JSON/CSV/RSS 中的原始文本不受影响。

2.  **分组边界标记**：open_group 和 close_group 两个布尔字段由 HTML 路径动态计算，用于模板中按模板类型分组渲染。它们不属于原始结果数据，也仅对 HTML 有意义。

> **代码出处**：[webutils.py#L162-L175](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webutils.py#L162-L175)（JSON 字段集合）、[webutils.py#L113-L146](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webutils.py#L113-L146)（CSV 固定 7 列）、[opensearch_response_rss.xml](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/templates/simple/opensearch_response_rss.xml)（RSS 字段）、[webapp.py#L702-L706](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L702-L706)（HTML 关键词高亮）、[_base.py#L339-L404](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/result_types/_base.py#L339-L404)（结果对象完整字段）

---

## 五、结果字段之外的附加信息

四种格式除了结果数据本身，还携带了数量不等的附加元数据和上下文信息。这些信息不是搜索结果条目，但对理解和使用结果至关重要。

### 5.1 JSON 的顶层元数据

JSON 是唯一在响应顶层结构化携带元数据的格式，顶层共有 8 个字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| query | 字符串 | 用户原始查询词 |
| number_of_results | 数值 | 结果总数估计 |
| results | 数组 | 主结果列表 |
| answers | 数组 | 直接答案列表 |
| corrections | 数组 | 拼写纠错字符串列表 |
| infoboxes | 数组 | 信息框列表 |
| suggestions | 数组 | 搜索建议字符串列表 |
| unresponsive_engines | 数组 | 翻译后的无响应引擎错误列表 |

JSON 的设计哲学是"完整镜像"——结果容器里有什么，JSON 里就输出什么，便于程序消费和二次开发。

### 5.2 CSV 的附加信息

CSV 几乎没有附加信息，只有第一行的表头：

```
title, url, content, host, engine, score, type
```

没有查询词，没有结果总数，没有时间戳。CSV 的设计哲学是"纯数据表格"——只放可以按行摊平的条目，其他一概省略。读者如果不结合请求上下文，根本不知道这张表对应的是什么搜索。

CSV 中用 `type` 列区分四种行类型：`result`（主结果）、`answer`（直接答案）、`suggestion`（搜索建议）、`correction`（拼写纠错）。四种类型挤在同一张表里，用 type 字段加以区分。

### 5.3 RSS 的 channel 级元数据

RSS 的元数据放在 channel 头部，遵循 OpenSearch 扩展规范：

| 元数据项 | 说明 |
|---------|------|
| channel/title | 搜索标题，格式为 "SearXNG search: 查询词" |
| channel/link | 搜索结果页的完整 URL |
| channel/description | 搜索描述文本 |
| opensearch:totalResults | 结果总数估计 |
| opensearch:startIndex | 起始索引，固定为 1 |
| opensearch:itemsPerPage | 每页结果数，值等于总数（非实际分页大小） |
| opensearch:Query | 查询元信息，包含查询词和起始页 |
| atom:link | OpenSearch 描述文档的链接 |

RSS 还附带了一个 XSL 样式表引用，可以在浏览器中直接呈现为美观的 HTML 页面。

RSS 的设计哲学是"标准订阅"——严格遵循 RSS 2.0 和 OpenSearch 规范，保证与所有 RSS 阅读器兼容，但也因此损失了大量 SearXNG 特有的信息。

### 5.4 HTML 的完整页面上下文

HTML 携带的附加信息最为丰富，远远不止结果数据本身。除了搜索结果，HTML 页面还包含以下上下文：

**搜索状态上下文**：
- 当前查询词
- 选中的分类列表
- 当前页码
- 时间范围
- 结果总数（格式化后的数字）
- 分页信息（总页数、是否有上一页/下一页）
- 当前语言设置、搜索语言设置

**辅助导航信息**：
- 搜索框（可重新搜索）
- 分类标签（可切换分类）
- 分页导航（可翻页）
- 搜索 URL（可复制分享）
- 下载入口（可切换格式）

**性能与状态信息**：
- 各引擎响应时间（从慢到快排序）
- 最大响应时间
- 无响应引擎列表（带翻译后的错误信息）
- 引擎数据键值对

**增强后的结果辅助信息**：
- 搜索建议（带跳转链接的结构化对象，而非原始字符串）
- 拼写纠错（带跳转链接的结构化对象，而非原始字符串）
- 直接答案列表
- 信息框列表

**完整 UI 框架**：
- 页头、页脚、侧栏
- 高级搜索表单
- 偏好设置入口
- 关于、隐私政策等静态页面链接

HTML 的设计哲学是"完整用户界面"——不仅展示结果，还提供搜索、导航、设置等完整交互能力。

> **代码出处**：[webapp.py#L756-L785](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L756-L785)（HTML 模板参数）、[webapp.py#L734-L751](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L734-L751)（建议/纠错/性能数据预处理）

---

## 六、完整链路的协作场景

通过几个典型场景可以看清各层如何联动：

### 场景一：侧栏按钮切换到 JSON

用户在 HTML 结果页点击 JSON 下载按钮：

1.  入口层：隐藏表单携带查询词、分类（多字段形式）、页码、语言、时间范围、安全搜索、超时限制 7 项搜索状态 + 目标格式
2.  参数合并：表单中的偏好值（安全搜索、语言等）覆盖 Cookie 中的持久化设置（仅本次请求生效）
3.  决策层：format=json 通过三道关口（合法格式名且在白名单中）
4.  序列化：JSON 格式完整输出 8 类顶层信息，每个结果保留全部 23 项字段，零信息损失

### 场景二：RSS 自动发现链接被访问

RSS 阅读器从页面头部发现订阅链接并发起 GET 请求：

1.  入口层：URL 查询字符串携带查询词、分类（逗号拼接形式）、页码、语言、时间范围、安全搜索 6 项搜索状态 + 目标格式（无超时限制）
2.  参数合并：GET 参数仅补充 POST 中没有的字段；由于这是纯 GET 请求，所有参数都生效
3.  决策层：format=rss 若被管理员禁用则直接 403（RSS 入口不感知白名单）
4.  序列化：RSS 格式输出 channel 元数据 + 主结果条目，每个结果仅保留 4 项字段，信息损失最大

### 场景三：管理员禁用 JSON 但用户手动拼接 URL 请求

用户通过 URL 手动加 `?format=json` 发起请求：

1.  入口层：无入口参与，格式参数来自 URL 查询字符串
2.  参数合并：GET 参数仅在 POST 没有时生效；纯 GET 请求则全部生效
3.  决策层：json 是合法格式名（通过关口二），但不在白名单中（关口三拦截）→ 返回 403
4.  序列化：链路中断，不执行任何序列化

---

## 七、设计评价

### 设计优点

1.  **信息分层合理**：JSON 面向程序完整保留，CSV 面向表格分析摊平简化，RSS 面向订阅遵循标准，HTML 面向用户提供完整交互。四种格式各有明确的受众定位，字段取舍与附加信息都与之匹配。

2.  **参数合并机制清晰**：普通参数与偏好参数走不同合并路径，POST 与 GET 的"主-补"关系、表单覆盖 Cookie 的偏好逻辑，都符合用户直觉。

3.  **入口与决策部分联动**：侧栏下载入口由格式白名单动态生成，保证可见即可用。

4.  **容错与控制分明**：三道关口语义清晰，手误与越权区别对待。

### 可改进点

1.  **RSS 自动发现的一致性问题**：RSS 链接硬编码不感知白名单，可能宣告一个 403 端点。应与侧栏入口保持一致，仅在 RSS 开放时渲染。

2.  **分类参数的双轨表达**：侧栏用 `category_xxx=1` 多字段，RSS 用 `categories=a,b` 单字段。两种表达方式增加了理解和维护成本，可考虑统一。

3.  **CSV 元数据过于匮乏**：没有查询词、没有时间戳、没有总数，导出的 CSV 文件脱离上下文几乎不可读。可考虑在文件头部增加注释行或元数据行。

4.  **入口状态传递不足**：单个引擎过滤、排序方式等高级状态在格式切换后丢失，对高级用户不够友好。

---

## 八、总结

SearXNG 的格式切换机制可以概括为三层协作体系：

- **参数来源与决策**：普通参数走 POST 主 GET 补的合并路径，偏好参数走"默认值→Cookie→表单→编码串"逐层覆盖的路径；格式值经过三道关口（默认值→非法回落→禁用拦截）裁决，手误与越权区别对待。

- **入口状态传递**：侧栏下载入口用 POST 表单保留 7 项状态（分类为多字段形式）且与白名单联动；RSS 自动发现链接用 GET URL 保留 6 项状态（分类为逗号拼接形式）但硬编码不感知白名单；两者传递的偏好值都会临时覆盖 Cookie 设置。

- **序列化信息映射**：JSON 是零损失的完整镜像（8 类顶层信息 × 23 项单结果字段）；CSV 固定 7 列纯数据表格，几乎没有附加元数据；RSS 遵循 OpenSearch 规范的 channel 元数据 + 4 字段正文，信息损失最大；HTML 是最完整的形态——除了所有结果类别和用户可见字段外，还携带搜索状态、导航、性能统计等完整页面上下文。

三层各司其职，构成了从"用户点击入口"到"最终格式响应"的完整链路。
