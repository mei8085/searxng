# SearXNG 输出格式切换机制分析

## 一、机制总览

SearXNG 支持四种搜索结果输出格式：HTML 页面、JSON 数据、CSV 表格和 RSS 订阅。同一份搜索结果可以在这四种格式之间切换，其核心是一套"格式来源与决策-入口状态传递-序列化字段映射"的三层协作机制。

格式来源与决策层决定格式参数从哪里来、优先级如何、三道关口怎样裁决；入口状态传递层决定下载按钮和 RSS 链接实际保留了哪些搜索上下文；序列化字段映射层决定四种格式各自真正保留和丢弃了哪些结果信息。

---

## 二、格式参数的来源与决策优先级

### 2.1 参数来源与合并顺序

搜索请求的参数来自多个层次，按照"低优先级先写入，高优先级后覆盖"的顺序逐层合并，最终形成请求参数字典。合并顺序如下：

1.  **HTTP 表单参数（POST Body）**：最先写入，是最低优先级的来源
2.  **URL 查询参数（GET Query String）**：随后写入，同名字段会覆盖表单参数
3.  **Cookie 中的持久化偏好**：随后写入，同名字段会覆盖前两者
4.  **编码后的偏好字符串**：如果请求携带了特殊的偏好编码字段，会解码后整体覆盖
5.  **浏览器语言头回退**：仅当以上来源都未提供语言和区域设置时，才从 `Accept-Language` 请求头推断

这意味着：用户在 URL 中手动加的 `?format=json` 会覆盖表单提交的格式值；而 Cookie 中保存的用户偏好（如安全搜索级别）会覆盖 URL 中同名字段——但 `format` 参数本身并不保存在 Cookie 中，所以它的实际竞争只在前两层之间展开。

> **代码出处**：[webapp.py#L484-L487](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L484-L487)（GET 覆盖 POST）、[webapp.py#L476-L476](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L476-L476)（Cookie 解析）、[webapp.py#L489-L496](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L489-L496)（偏好编码覆盖）、[webapp.py#L500-L502](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L500-L502)（Accept-Language 回退）

### 2.2 格式决策的三道关口

从请求参数字典中取出 `format` 值后，会依次经过三道决策关口，每道关口的语义截然不同：

| 关口 | 触发条件 | 处理方式 | 设计意图 |
|------|---------|---------|---------|
| 参数提取与默认值 | 未携带 format 参数 | 默认为 HTML | 面向无声明场景的合理默认 |
| 非法格式回落 | 格式名不在四种硬编码格式中 | 静默重置为 HTML | 面向未知输入的容错降级，"写错了"得到宽容 |
| 配置禁用拦截 | 格式名合法但不在管理员配置的白名单中 | 返回 403 中断请求 | 面向运维策略的访问控制，"越权了"必须拒绝 |

**关键的顺序问题**：非法格式回落发生在配置禁用拦截之前。这意味着如果管理员只开放了 HTML，用户请求一个不存在的格式（如 `yaml`）会先被修正为 `html`，然后顺利通过拦截——用户看到正常的搜索结果页，根本不知道自己的参数被改写过。只有当用户请求一个合法但被禁用的格式（如 `json`）时，才会触发 403。

换言之：**写错了格式名得到宽容，写对了但没权限被严格拦截**。这是一个刻意的设计选择，将"手误"和"越权"区别对待。

> **代码出处**：[webapp.py#L629-L634](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L629-L634)（三道关口）、[settings_defaults.py#L22](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/settings_defaults.py#L22-L22)（硬编码格式列表）、[settings_defaults.py#L207](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/settings_defaults.py#L207-L207)（格式白名单配置）

---

## 三、格式入口的搜索状态保留

SearXNG 有两种格式入口：侧栏下载入口和 RSS 自动发现链接。它们各自保留的搜索上下文并不完全相同。

### 3.1 侧栏下载入口：保留的状态

HTML 结果页侧栏的每个下载按钮都是一个隐藏表单，通过表单字段将当前搜索的上下文传递给新的格式请求。实际保留的状态包括：

| 状态项 | 说明 |
|--------|------|
| 查询词 | 完整保留用户输入的搜索关键词 |
| 分类过滤 | 逐个字段传递，每个选中的分类以 `category_xxx=1` 的形式保留 |
| 页码 | 保留当前页码，分页状态在格式切换后延续 |
| 界面语言 | 保留当前语言设置 |
| 时间范围 | 保留时间过滤条件 |
| 安全搜索级别 | 保留安全搜索过滤强度 |
| 超时限制 | 若有自定义超时则保留，否则不传 |

**不保留的状态**：

- 单个引擎的启用/禁用状态（只保留分类级别的过滤）
- 插件的启用/禁用状态
- 主题、布局等纯 UI 偏好
- 结果排序方式（始终使用默认排序）

### 3.2 RSS 自动发现链接：保留的状态

HTML 页面头部的 RSS 订阅链接是一个构造好的 URL 查询字符串，它实际保留的状态包括：

| 状态项 | 说明 |
|--------|------|
| 查询词 | 完整保留并做 URL 编码 |
| 分类过滤 | 用逗号拼接成一个 `categories` 参数 |
| 页码 | 保留当前页码 |
| 时间范围 | 保留 |
| 界面语言 | 保留 |
| 安全搜索级别 | 保留 |

**不保留的状态**：

- 超时限制（与下载入口不同，RSS 链接从不传递超时参数）
- 与下载入口一致，不保留单个引擎状态、插件状态和 UI 偏好

### 3.3 两者的差异对比

| 维度 | 侧栏下载入口 | RSS 自动发现链接 |
|------|-------------|-----------------|
| 传递方式 | POST 表单隐藏字段 | GET URL 查询字符串 |
| 分类表达 | 多字段 `category_xxx=1` | 单字段 `categories=a,b,c` |
| 超时限制 | 可选传递 | 从不传递 |
| 白名单联动 | 动态生成，白名单之外不出现 | 硬编码，不受白名单控制 |
| 与 Cookie 偏好协作 | 受 Cookie 层覆盖影响 | 受 Cookie 层覆盖影响 |

**关于 Cookie 覆盖的重要提醒**：无论下载入口和 RSS 链接构造了多少参数，Cookie 中保存的用户偏好（安全搜索、语言、分类等）都会在参数合并阶段覆盖入口传递的值。这意味着入口保留的状态只是"请求时的建议值"，最终生效的值还取决于用户 Cookie。

> **代码出处**：[apis.html](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/templates/simple/elements/apis.html)（侧栏下载入口模板）、[results.html#L11](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/templates/simple/results.html#L11-L11)（RSS 自动发现链接）

---

## 四、四种格式的结果信息映射

搜索结果容器中包含丰富的信息：主结果、直接答案、搜索建议、拼写纠错、信息框、无响应引擎、性能统计、每个结果的几十项元数据字段。四种格式各自从这个容器中取走不同的子集，丢掉的信息也各不相同。

### 4.1 顶层结果类别的映射

| 结果类别 | 说明 | JSON | CSV | RSS | HTML |
|---------|------|:----:|:---:|:---:|:----:|
| 主结果列表 | 搜索引擎返回的核心结果 | ✓ | ✓ | ✓ | ✓ |
| 直接答案 | 计算器、翻译、天气等即时回答 | ✓ | ✓ | ✗ | ✓ |
| 搜索建议 | "您是不是要搜"的建议词 | ✓ | ✓ | ✗ | ✓ |
| 拼写纠错 | 拼写修正建议 | ✓ | ✓ | ✗ | ✓ |
| 信息框 | 维基百科摘要等信息框 | ✓ | ✗ | ✗ | ✓ |
| 无响应引擎 | 超时或出错的引擎列表 | ✓ | ✗ | ✗ | ✓ |
| 结果总数估计 | 命中结果总数 | ✓ | ✗ | ✓ | ✓ |
| 查询词原文 | 用户输入的搜索词 | ✓ | ✗ | ✓ | ✓ |

**解读**：
- JSON 是信息最全的格式，顶层八大类别全部保留
- CSV 只保留了"可以按行摊平"的四类（主结果、答案、建议、纠错），其余一概丢弃
- RSS 只保留了主结果，丢失了所有辅助信息，甚至总数也是写在元数据而非正文中
- HTML 除了 CSV 丢掉的无响应引擎不直接显示在正文（显示在页脚提示）外，其余全部保留

### 4.2 单个结果的字段映射

每个主结果对象包含三十余项字段，四种格式对单个结果字段的取舍差异更大。下表列出主要字段的保留情况：

| 字段 | 说明 | JSON | CSV | RSS | HTML |
|------|------|:----:|:---:|:---:|:----:|
| title | 结果标题 | ✓ | ✓ | ✓ | ✓ |
| url | 结果链接 | ✓ | ✓ | ✓ | ✓ |
| content | 结果摘要 | ✓ | ✓ | ✓ | ✓ |
| engine | 来源引擎名 | ✓ | ✓ | ✗ | ✓ |
| engines | 合并后所有来源引擎 | ✓ | ✗ | ✗ | ✓ |
| score | 结果排序分数 | ✓ | ✓ | ✗ | ✗ |
| parsed_url | 结构化解析的 URL | ✓ | 仅主机名 | ✗ | ✓ |
| publishedDate | 发布日期（日期对象） | ✓ | ✗ | ✗ | ✓ |
| pubdate | 发布日期字符串 | ✓ | ✗ | ✓ | ✓ |
| category | 结果所属分类 | ✓ | ✗ | ✗ | ✓ |
| template | 使用的渲染模板名 | ✓ | ✗ | ✗ | 内部使用 |
| thumbnail | 缩略图 URL | ✓ | ✗ | ✗ | ✓ |
| img_src | 图片源 URL | ✓ | ✗ | ✗ | ✓ |
| author | 作者 | ✓ | ✗ | ✗ | ✓ |
| metadata | 杂项元数据 | ✓ | ✗ | ✗ | ✓ |
| views | 观看次数 | ✓ | ✗ | ✗ | ✓ |
| length | 时长（音视频） | ✓ | ✗ | ✗ | ✓ |
| iframe_src | 嵌入视频 URL | ✓ | ✗ | ✗ | ✓ |
| audio_src | 音频源 URL | ✓ | ✗ | ✗ | ✓ |
| priority | 结果优先级标记 | ✓ | ✗ | ✗ | ✗ |
| positions | 各引擎中的排名 | ✓ | ✗ | ✗ | ✗ |
| open_group | 分组开启标记 | ✓ | ✗ | ✗ | 内部使用 |
| close_group | 分组关闭标记 | ✓ | ✗ | ✗ | 内部使用 |

**解读**：

- **JSON**：完整保留全部 23 项字段，是唯一能还原原始结果对象的格式，同时也是唯一暴露排序分数、排名位置、优先级等内部元数据的格式
- **CSV**：固定 7 列，取了最通用的几项。URL 字段被拆解后只保留主机名（放入独立的 host 列），而完整 URL 仍保留在 url 列。单个结果中丰富的媒体字段、作者、元数据等全部丢弃
- **RSS**：只有 title、url、content、pubdate 四项。来源引擎、分数、分类、媒体资源等全部丢失。RSS 是信息损失最大的格式
- **HTML**：实际展示的字段介于 JSON 和 RSS 之间，所有面向用户的字段都会显示（作者、缩略图、嵌入媒体等），但排序分数、排名位置等内部调试信息不暴露给用户

### 4.3 HTML 格式特有的字段转换

HTML 除了字段取舍外，还对两个字段做了不可还原的格式转换：

1.  **关键词高亮**：title 和 content 字段会对搜索关键词做 HTML 标记插入，在关键词周围包裹 `<span class="highlight">` 标签。同时 content 会被截断到 1024 字符。这个转换只发生在 HTML 路径，JSON/CSV/RSS 中的原始文本不受影响。

2.  **分组边界标记**：open_group 和 close_group 两个布尔字段由 HTML 路径动态计算，用于模板中按模板类型分组渲染。它们不属于原始结果数据，也仅对 HTML 有意义。

> **代码出处**：[webutils.py#L162-L175](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webutils.py#L162-L175)（JSON 字段集合）、[webutils.py#L113-L146](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webutils.py#L113-L146)（CSV 固定 7 列）、[opensearch_response_rss.xml](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/templates/simple/opensearch_response_rss.xml)（RSS 四项字段）、[webapp.py#L702-L706](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/webapp.py#L702-L706)（HTML 关键词高亮）、[_base.py#L339-L404](file:///d:/fz/0601-1/solo-dogfeeding/code/40-searxng/searx/result_types/_base.py#L339-L404)（结果对象完整字段）

---

## 五、完整链路的协作场景

通过几个典型场景可以看清各层如何联动：

### 场景一：侧栏按钮切换到 JSON

用户在 HTML 结果页点击 JSON 下载按钮：

1.  入口层：隐藏表单携带查询词、分类、页码、语言、时间范围、安全搜索、超时限制 7 项状态
2.  参数合并：Cookie 中的持久化偏好可能覆盖入口传递的同名字段
3.  决策层：format=json 通过三道关口（合法格式名且在白名单中）
4.  序列化：JSON 格式完整输出 8 类顶层结果、23 项单结果字段，零信息损失

### 场景二：RSS 自动发现链接被访问

RSS 阅读器从页面头部发现订阅链接并发起请求：

1.  入口层：URL 查询字符串携带查询词、分类、页码、语言、时间范围、安全搜索 6 项状态（无超时限制）
2.  参数合并：Cookie 偏好覆盖入口值
3.  决策层：format=rss 若被管理员禁用则直接 403（RSS 入口不感知白名单）
4.  序列化：RSS 格式只输出主结果，且每个结果仅保留 4 项字段，信息损失最大

### 场景三：管理员配置禁用 JSON 但用户手动拼接请求

用户通过 URL 手动加 `?format=json` 发起请求：

1.  入口层：无入口参与，格式参数来自 URL 查询字符串
2.  参数合并：URL 参数覆盖任何表单参数，但受 Cookie 层覆盖
3.  决策层：json 是合法格式名（通过关口二），但不在白名单中（关口三拦截）→ 返回 403
4.  序列化：链路中断，不执行任何序列化

---

## 六、设计评价

### 设计优点

1.  **信息分层合理**：JSON 面向程序完整保留，CSV 面向表格分析摊平简化，RSS 面向订阅只留核心，HTML 面向用户按需展示。四种格式各有明确的受众定位，字段取舍与之匹配。
2.  **参数合并机制清晰**：多层来源按优先级合并，既允许入口传递上下文，又保证用户持久化偏好不被入口覆盖。
3.  **入口与决策联动（侧栏）**：下载入口由格式白名单动态生成，保证可见即可用。
4.  **容错与控制分明**：三道关口语义清晰，手误与越权区别对待。

### 可改进点

1.  **RSS 自动发现的一致性问题**：RSS 链接硬编码不感知白名单，可能宣告 403 端点。应与侧栏入口保持一致，仅在 RSS 开放时渲染。
2.  **CSV 字段过于精简**：仅 7 列固定模式，丢失了作者、日期、媒体等对表格分析也有价值的字段。可考虑可配置列或多套 CSV 模板。
3.  **入口缺少更多状态传递**：单个引擎过滤、排序方式等状态在格式切换后丢失，对高级用户不够友好。
4.  **RSS 字段过于贫乏**：RSS 2.0 规范支持很多可选字段（分类、作者、来源、缩略图 enclosure 等），当前只利用了最少四项，信息损失可进一步降低。

---

## 七、总结

SearXNG 的格式切换机制可以概括为三层协作体系：

- **格式来源与决策**：参数来自多层（表单→查询串→Cookie→偏好编码→Accept-Language）按优先级合并；格式值经过三道关口（默认值→非法回落→禁用拦截）裁决，手误与越权区别对待。
- **入口状态传递**：侧栏下载入口用表单保留 7 项状态且与白名单联动；RSS 自动发现链接用 URL 保留 6 项状态但硬编码不感知白名单；两者传递的状态值最终都可能被 Cookie 覆盖。
- **序列化字段映射**：JSON 是零损失的完整镜像（8 类顶层 × 23 项字段）；CSV 固定 7 列适合表格分析；RSS 仅 4 项字段信息损失最大；HTML 面向用户展示所有可见字段并做关键词高亮转换。

三层各司其职，构成了从"用户点击入口"到"最终格式响应"的完整链路。
